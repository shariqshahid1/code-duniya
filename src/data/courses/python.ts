import { Course } from "@/types"

export const pythonCourse: Course = {
  id: "python", title: "Python",
  description: "Python seekhein - Programming fundamentals master karein",
  longDescription: "Python ek versatile programming language hai jo beginner-friendly aur powerful dono hai. Is course mein aap Python basics, data structures, OOP, file handling, aur projects seekhenge.",
  category: "programming-languages", icon: "Terminal", color: "from-yellow-500 to-blue-500",
  difficulty: "beginner", lessonsCount: 10, duration: "5 hours",
  prerequisites: ["Koi programming experience zaroori nahi"],
  modules: [{
    id: "python-core", title: "Python Basics", description: "Python fundamentals", order: 1, lessons: [
      { id: "py-what-is", title: "Python Kya Hai?", description: "Python introduction", order: 1, content: { introduction: "Python ek high-level programming language hai jo readability aur simplicity ke liye famous hai. Iska use web development, data science, AI, automation mein hota hai.", sections: [
        { title: "Python Overview", explanation: "Python Guido van Rossum ne 1991 mein banaya. Simple syntax (English jaisa). Interpreted language (line by line execute). Dynamically typed. Large standard library. Cross-platform.", example: "print('Hello World') - ye poora Python program hai!" },
      ], codeExamples: [
        { title: "Pehla Python Program", language: "python", code: "# Pehla Python program\nprint('Namaste Duniya!')\n\n# Variables\nname = 'Rahul'\nage = 25\nprint(f'Mera naam {name} hai aur main {age} saal ka hoon')\n\n# Simple calculation\na = 10\nb = 20\nprint(f'Sum: {a + b}')", output: "Python mein simple program." },
      ], notes: ["Python simple aur readable language hai", "Interpreted language (line by line execute)", "Dynamically typed - type declare nahi karna padta", "Large standard library available"],
      commonMistakes: [{ mistake: "Indentation galat karna", correction: "Python mein indentation (4 spaces) compulsory hai" }],
      interviewQuestions: ["Python kya hai?", "Interpreted aur compiled language mein kya antar hai?", "Python ke kya fayde hain?"],
      practiceExercises: [{ question: "Python program likhein jo aapka naam, age aur city print kare.", difficulty: "easy" }],
      quiz: { id: "quiz-py-intro", title: "Python Intro Quiz", questions: [
        { id: "q1", question: "Python ko kisne banaya?", options: ["Bill Gates", "Guido van Rossum", "Tim Berners-Lee", "Brendan Eich"], correctAnswer: 1, explanation: "Guido van Rossum ne Python banaya." },
      ]}, }, duration: "15 min" },
      { id: "py-variables", title: "Variables aur Data Types", description: "Python variables aur types", order: 2, content: { introduction: "Python variables dynamically typed hote hain - aap type specify nahi karte. Python interpreter automatic type deduce karta hai.", sections: [
        { title: "Data Types", explanation: "int (integers), float (decimals), str (strings), bool (True/False), list ([ ]), tuple (()), dict ({ }), set ({ }). type() function se type check karte hain. type() conversion functions.", example: "x = 10 (int), y = 3.14 (float), name = 'Rahul' (str), is_student = True (bool)" },
        { title: "Type Conversion", explanation: "int(), float(), str(), bool() functions se explicit conversion. str() + int() concatenation? Error - pehle str() mein convert karo. input() hamesha string return karta hai.", example: "age = int(input('Enter age: '))" },
      ], codeExamples: [
        { title: "Variables Example", language: "python", code: "# Variable assignment\nname = 'Rahul'\nage = 25\nheight = 5.9\nis_student = True\n\n# Type checking\nprint(type(name))  # <class 'str'>\nprint(type(age))   # <class 'int'>\n\n# Type conversion\nage_str = str(age)\nprint('Age is: ' + age_str)  # concatenation\n\n# Input (always returns string)\nuser_input = input('Kuch likhein: ')\nprint('Aapne likha:', user_input)\n\n# Multiple assignment\nx, y, z = 1, 2, 3\nprint(x, y, z)\n\n# Swap\nx, y = y, x\nprint(x, y)", output: "Variables, types, input, type conversion." },
      ], notes: ["Python dynamically typed hai", "input() hamesha string return karta hai", "int()/float() se conversion karein", "f-strings best way to format: f'Hello {name}'"],
      commonMistakes: [{ mistake: "input() ko integer expect karke without int() use karna", correction: "int(input()) use karein for numeric input" }],
      interviewQuestions: ["Dynamically typed language kya hai?", "Python data types kaun se hain?"],
      practiceExercises: [{ question: "Do numbers input lekar unka sum, difference, product aur quotient print karein.", difficulty: "easy" }],
      quiz: { id: "quiz-py-vars", title: "Variables Quiz", questions: [
        { id: "q1", question: "Type check karne ke liye kaunsa function use hota hai?", options: ["checktype()", "typeof()", "type()", "isinstance()"], correctAnswer: 2, explanation: "type() function type check karta hai." },
      ]}, }, duration: "15 min" },
      { id: "py-strings", title: "Strings aur String Methods", description: "Python strings aur methods", order: 3, content: { introduction: "Strings characters ka sequence hote hain. Python mein strings ke bahut se built-in methods hain jinse aap aasani se text manipulate kar sakte hain.", sections: [
        { title: "String Operations", explanation: "Concatenation (+), Repetition (*), Indexing (str[0]), Slicing (str[1:4]), Negative indexing. len() function. f-strings: f'Hello {name}'. Multiline strings with ''' or \"\"\".", example: "name = 'Rahul'; print(f'Hello {name}, age: {age}')" },
        { title: "String Methods", explanation: ".upper(), .lower(), .capitalize(), .title() for case. .strip() - whitespace remove. .split() - list mein convert. .join() - list se string. .replace(old, new). .find(), .count(), .startswith(), .endswith().", example: "'hello world'.split() -> ['hello', 'world']; '-'.join(['a', 'b']) -> 'a-b'" },
      ], codeExamples: [
        { title: "Strings Example", language: "python", code: "# String basics\ns = 'Hello Python'\nprint(s[0])     # H\nprint(s[0:5])   # Hello\nprint(s[::-1])  # Reverse: nohtyP olleH\n\n# f-strings\nname = 'Rahul'\nage = 25\nprint(f'My name is {name} and I am {age} years old')\n\n# Methods\ntext = '  Python Programming  '\nprint(text.strip())\nprint(text.strip().upper())\nprint(text.strip().replace('Python', 'Java'))\n\n# Split and join\ncsv = 'apple,banana,mango'\nfruits = csv.split(',')\nprint(fruits)  # ['apple', 'banana', 'mango']\nprint(' | '.join(fruits))\n\n# Check\nemail = 'test@example.com'\nif '@' in email and email.endswith('.com'):\n    print('Valid email')", output: "String operations, methods, f-strings examples." },
      ], notes: ["Strings immutable hote hain (change nahi ho sakte)", "f-strings best way to format strings", "Negative indexing reverse se count karta hai", "split() aur join() important methods hain"],
      commonMistakes: [{ mistake: "String aur number directly concatenate karna", correction: "str() mein convert karein ya f-strings use karein: f'Age: {age}'" }],
      interviewQuestions: ["Strings immutable kya hota hai?", "f-strings kya hain?", "split() aur join() kaise kaam karte hain?"],
      practiceExercises: [{ question: "User se ek sentence input lekar uske words count karein aur reverse order mein print karein.", difficulty: "medium" }],
      quiz: { id: "quiz-py-strings", title: "Strings Quiz", questions: [
        { id: "q1", question: "String ko reverse karne ke liye kaunsa syntax use hota hai?", options: ["str.reverse()", "str[::-1]", "reverse(str)", "str[-1]"], correctAnswer: 1, explanation: "[::-1] se string reverse hoti hai." },
      ]}, }, duration: "15 min" },
      { id: "py-lists", title: "Lists aur Tuples", description: "Python lists aur tuples", order: 4, content: { introduction: "Lists ordered, mutable collections hain. Tuples ordered, immutable collections hain. Dono multiple items store kar sakte hain.", sections: [
        { title: "List Operations", explanation: "Creating: [1, 2, 3] or list(). Indexing, slicing. Methods: .append(), .insert(), .remove(), .pop(), .sort(), .reverse(). List comprehension: [x*2 for x in range(10)].", example: "squares = [x**2 for x in range(1, 6)]  # [1, 4, 9, 16, 25]" },
        { title: "Tuples aur Sets", explanation: "Tuples: (1, 2, 3) - immutable, faster than lists. Sets: {1, 2, 3} - unordered, unique items. Union, intersection, difference operations. Frozen set - immutable set.", example: "tuple = (1, 2, 3); set = {1, 2, 2, 3}  # {1, 2, 3}" },
      ], codeExamples: [
        { title: "Lists Example", language: "python", code: "# Lists\nfruits = ['apple', 'banana', 'mango']\nfruits.append('orange')\nfruits.insert(0, 'grape')\nfruits.remove('banana')\npopped = fruits.pop()\nprint(fruits)     # ['grape', 'apple', 'mango']\nprint(popped)     # orange\n\n# List comprehension\nnumbers = [1, 2, 3, 4, 5]\nsquares = [n**2 for n in numbers]\neven = [n for n in numbers if n % 2 == 0]\nprint(squares)  # [1, 4, 9, 16, 25]\nprint(even)     # [2, 4]\n\n# Tuples (immutable)\npoint = (10, 20)\nx, y = point  # unpacking\nprint(f'X: {x}, Y: {y}')\n\n# Sets\nunique = {1, 2, 2, 3, 3, 4}\nprint(unique)  # {1, 2, 3, 4}\n\n# Set operations\na = {1, 2, 3}\nb = {3, 4, 5}\nprint(a | b)  # union: {1, 2, 3, 4, 5}\nprint(a & b)  # intersection: {3}", output: "Lists, tuples, sets ke operations." },
      ], notes: ["Lists mutable hain (change ho sakte hain)", "Tuples immutable hain", "List comprehension powerful feature hai", "Sets unique items store karte hain"],
      commonMistakes: [{ mistake: "List ko default argument mein use karna (mutable default)", correction: "None use karo aur function mein initialize karo" }],
      interviewQuestions: ["List aur tuple mein kya antar hai?", "List comprehension kya hai?", "Set vs list kab use karein?"],
      practiceExercises: [{ question: "List comprehension se 1-50 ke even numbers ki list banayein. Uske baad us list se 7 se divisible numbers filter karein.", difficulty: "medium" }],
      quiz: { id: "quiz-py-lists", title: "Lists Quiz", questions: [
        { id: "q1", question: "List ke end mein element add karne ke liye kaunsa method use hota hai?", options: [".add()", ".append()", ".insert()", ".push()"], correctAnswer: 1, explanation: ".append() list ke end mein add karta hai." },
      ]}, }, duration: "15 min" },
      { id: "py-conditionals", title: "Conditionals aur Loops", description: "If-else, for, while loops", order: 5, content: { introduction: "Control flow statements program mein decisions lene aur repetitive tasks karne ke liye use hote hain.", sections: [
        { title: "If-Else", explanation: "if condition: elif condition: else:. Python mein indentation blocks define karti hai. Conditional operators: ==, !=, <, >, <=, >=. Logical: and, or, not. Ternary: x if condition else y.", example: "if age >= 18: print('Adult'); else: print('Minor')" },
        { title: "Loops", explanation: "for item in iterable: - collection par iterate. while condition: - condition true rehne tak. range(start, stop, step). enumerate() - index + value. break - exit loop. continue - skip iteration. else clause in loops.", example: "for i, fruit in enumerate(fruits): print(i, fruit)" },
      ], codeExamples: [
        { title: "Control Flow", language: "python", code: "# If-elif-else\nscore = 85\ngrade = 'A' if score >= 90 else 'B' if score >= 80 else 'C'\nprint(f'Grade: {grade}')\n\n# For loop\nfruits = ['apple', 'banana', 'mango']\nfor fruit in fruits:\n    print(fruit)\n\n# For with enumerate\nfor idx, fruit in enumerate(fruits, 1):\n    print(f'{idx}. {fruit}')\n\n# Range\nfor i in range(1, 6):\n    print(i, end=' ')  # 1 2 3 4 5\n\n# While loop\ncount = 0\nwhile count < 5:\n    print(count)\n    count += 1\n\n# Break and continue\nfor i in range(10):\n    if i == 3:\n        continue  # skip 3\n    if i == 7:\n        break     # stop at 7\n    print(i, end=' ')  # 0 1 2 4 5 6", output: "Control flow examples - if-else, for, while, break, continue." },
      ], notes: ["Indentation Python mein blocks define karti hai", "4 spaces standard indentation hai", "for item in iterable syntax", "range() numbers generate karta hai"],
      commonMistakes: [{ mistake: "Indentation ka dhyan na rakhna (IndentationError)", correction: "Consistent indentation (4 spaces) use karein" }],
      interviewQuestions: ["Range function ka syntax kya hai?", "Break aur continue mein kya antar hai?", "enumerate() kya karta hai?"],
      practiceExercises: [{ question: "FizzBuzz program likhein: 1-100 tak, 3 se divisible 'Fizz', 5 se 'Buzz', dono se 'FizzBuzz'.", difficulty: "medium" }],
      quiz: { id: "quiz-py-control", title: "Control Flow Quiz", questions: [
        { id: "q1", question: "Loop se bahar nikalne ke liye kaunsa keyword use hota hai?", options: ["exit", "break", "continue", "stop"], correctAnswer: 1, explanation: "break loop exit karta hai." },
      ]}, }, duration: "15 min" },
      { id: "py-functions", title: "Functions", description: "Python functions aur modules", order: 6, content: { introduction: "Functions reusable code blocks hain. Python mein functions first-class objects hain - aap unhe variables mein store, arguments mein pass, aur return kar sakte hain.", sections: [
        { title: "Function Definition", explanation: "def function_name(params):. Return statement. Default parameters: def f(a, b=5). Keyword arguments: f(b=3, a=1). *args - variable positional arguments. **kwargs - variable keyword arguments.", example: "def greet(name='Guest'): return f'Hello {name}'" },
        { title: "Lambda Functions", explanation: "lambda args: expression. Anonymous functions. Single expression. map(), filter(), reduce() ke saath commonly use. sorted() key function.", example: "square = lambda x: x**2; print(square(5))" },
      ], codeExamples: [
        { title: "Functions Example", language: "python", code: "# Basic function\ndef add(a, b):\n    return a + b\n\nprint(add(5, 3))  # 8\n\n# Default parameter\ndef greet(name='Guest'):\n    return f'Hello {name}'\n\nprint(greet('Rahul'))  # Hello Rahul\nprint(greet())         # Hello Guest\n\n# *args and **kwargs\ndef sum_all(*args):\n    return sum(args)\n\nprint(sum_all(1, 2, 3, 4))  # 10\n\ndef print_info(**kwargs):\n    for key, value in kwargs.items():\n        print(f'{key}: {value}')\n\nprint_info(name='Rahul', age=25, city='Delhi')\n\n# Lambda functions\nsquare = lambda x: x**2\nprint(square(5))  # 25\n\n# map, filter, sorted\nnumbers = [1, 2, 3, 4, 5]\ndoubled = list(map(lambda x: x*2, numbers))\neven = list(filter(lambda x: x%2==0, numbers))\nprint(doubled)  # [2, 4, 6, 8, 10]\nprint(even)     # [2, 4]\n\n# Decorators (advanced)\ndef timer(func):\n    import time\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        print(f'{func.__name__} took {time.time()-start:.2f}s')\n        return result\n    return wrapper\n\n@timer\ndef slow_function():\n    import time\n    time.sleep(1)\n    return 'Done'\n\nprint(slow_function())", output: "Functions, args/kwargs, lambda, map/filter, decorators." },
      ], notes: ["def se function define karte hain", "*args variable arguments, **kwargs variable keyword args", "lambda anonymous functions ke liye", "Decorators functions modify karte hain"],
      commonMistakes: [{ mistake: "Mutable default arguments (list/dict) ka side effect", correction: "None default karein aur function mein initialize karein" }],
      interviewQuestions: ["*args aur **kwargs kya hain?", "Lambda function kya hai?", "Decorator kya hai?"],
      practiceExercises: [{ question: "Decorator banaye jo function ke execution time measure kare. Fibonacci function par apply karein.", difficulty: "hard" }],
      quiz: { id: "quiz-py-functions", title: "Functions Quiz", questions: [
        { id: "q1", question: "Variable keyword arguments ke liye kaunsa parameter use hota hai?", options: ["*args", "**kwargs", "*kwargs", "**args"], correctAnswer: 1, explanation: "**kwargs variable keyword arguments ke liye." },
      ]}, }, duration: "20 min" },
      { id: "py-oop", title: "Object-Oriented Programming", description: "Python classes aur OOP", order: 7, content: { introduction: "Python OOP fully support karta hai. Classes, objects, inheritance, polymorphism sab features hain. Sab kuch Python mein object hai.", sections: [
        { title: "Classes aur Objects", explanation: "class ClassName: se class define. __init__ method constructor. self parameter instance reference. Instance method, class method (@classmethod), static method (@staticmethod). __str__, __repr__ string representation.", example: "class Person: def __init__(self, name): self.name = name" },
        { title: "Inheritance", explanation: "class Child(Parent):. super().__init__() - parent constructor. Method overriding. Multiple inheritance (class C(A, B)). MRO (Method Resolution Order). isinstance() and issubclass().", example: "class Student(Person): def __init__(self, name, grade): super().__init__(name); self.grade = grade" },
      ], codeExamples: [
        { title: "OOP Example", language: "python", code: "class Animal:\n    def __init__(self, name):\n        self.name = name\n    \n    def speak(self):\n        raise NotImplementedError\n    \n    def __str__(self):\n        return f'{self.name}'\n\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n    \n    def speak(self):\n        return f'{self.name} says Woof!'\n\n\nclass Cat(Animal):\n    def speak(self):\n        return f'{self.name} says Meow!'\n\n\n# Polymorphism\ndef make_sound(animal):\n    print(animal.speak())\n\n\nanimals = [Dog('Tommy', 'Golden'), Cat('Kitty')]\nfor animal in animals:\n    make_sound(animal)\n\n# Simple inheritance\nprint(isinstance(Dog('A', 'B'), Animal))  # True\nprint(issubclass(Dog, Animal))           # True\n\n# Dunder methods\nclass Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    \n    def __add__(self, other):\n        return Point(self.x + other.x, self.y + other.y)\n    \n    def __str__(self):\n        return f'Point({self.x}, {self.y})'\n\np1 = Point(1, 2)\np2 = Point(3, 4)\nprint(p1 + p2)  # Point(4, 6) due to __add__", output: "OOP concepts - classes, inheritance, polymorphism, dunder methods." },
      ], notes: ["__init__ constructor hai", "self instance reference hai", "super() parent class access karta hai", "__str__ aur __repr__ string representation ke liye"],
      commonMistakes: [{ mistake: "__init__ ke parameter mein self bhoolna", correction: "self pehla parameter hamesha dena" }],
      interviewQuestions: ["__init__ kya hai?", "Classmethod aur staticmethod mein kya antar hai?", "Multiple inheritance kaise kaam karta hai?"],
      practiceExercises: [{ question: "BankAccount class banaye with deposit, withdraw, balance methods. SavingsAccount subclass banaye with interest rate.", difficulty: "medium" }],
      quiz: { id: "quiz-py-oop", title: "OOP Quiz", questions: [
        { id: "q1", question: "Python mein constructor kaunsa method hai?", options: ["__construct__", "__init__", "__new__", "__create__"], correctAnswer: 1, explanation: "__init__ constructor method hai." },
      ]}, }, duration: "20 min" },
      { id: "py-files", title: "File Handling", description: "Python file I/O operations", order: 8, content: { introduction: "File handling Python mein important feature hai. Files read karna, write karna, append karna aur binary files handle karna seekhenge.", sections: [
        { title: "File Operations", explanation: "open() function. Modes: 'r' (read), 'w' (write, overwrite), 'a' (append), 'rb'/'wb' (binary). with statement (context manager) - file auto close. read(), readline(), readlines(). write(), writelines().", example: "with open('file.txt', 'r') as f: content = f.read()" },
        { title: "Exception Handling", explanation: "try-except-else-finally. Specific exceptions (FileNotFoundError, PermissionError). raise statement. Custom exceptions. Multiple except blocks.", example: "try: f = open('file.txt'); except FileNotFoundError: print('File not found')" },
      ], codeExamples: [
        { title: "File Handling", language: "python", code: "# Writing to file\nwith open('data.txt', 'w') as f:\n    f.write('Hello World\\n')\n    f.write('This is line 2\\n')\n\n# Append to file\nwith open('data.txt', 'a') as f:\n    f.write('This is appended line\\n')\n\n# Reading file\nwith open('data.txt', 'r') as f:\n    content = f.read()\n    print('Full content:')\n    print(content)\n\n# Read line by line\nwith open('data.txt', 'r') as f:\n    for line in f:\n        print(f'Line: {line.strip()}')\n\n# Read all lines to list\nwith open('data.txt', 'r') as f:\n    lines = f.readlines()\n    print(f'Total lines: {len(lines)}')\n\n# Exception handling\ntry:\n    with open('nonexistent.txt', 'r') as f:\n        content = f.read()\nexcept FileNotFoundError:\n    print('File nahi mila!')\nexcept PermissionError:\n    print('Permission nahi hai!')\nexcept Exception as e:\n    print(f'Koi aur error: {e}')\nelse:\n    print('File successfully read')\nfinally:\n    print('Ye hamesha chalega')\n\n# Working with JSON\nimport json\n\ndata = {'name': 'Rahul', 'age': 25, 'city': 'Delhi'}\n\n# Write JSON\nwith open('data.json', 'w') as f:\n    json.dump(data, f, indent=2)\n\n# Read JSON\nwith open('data.json', 'r') as f:\n    loaded = json.load(f)\n    print(loaded['name'])", output: "File I/O, context manager, exception handling, JSON." },
      ], notes: ["with statement se file auto close hoti hai", "Modes: r (read), w (write), a (append)", "try-except se errors handle karein", "json module JSON data handle karta hai"],
      commonMistakes: [{ mistake: "File ko close karna bhoolna (use 'with' always)", correction: "'with' statement use karein for auto close" }],
      interviewQuestions: ["With statement kya hai?", "File modes kaun se hain?", "JSON read/write kaise karte hain?"],
      practiceExercises: [{ question: "User se input lekar file mein save karein, phir file se read karke display karein. Error handling implement karein.", difficulty: "medium" }],
      quiz: { id: "quiz-py-files", title: "File Handling Quiz", questions: [
        { id: "q1", question: "File read karne ka mode kaunsa hai?", options: ["'w'", "'r'", "'a'", "'x'"], correctAnswer: 1, explanation: "'r' read mode hai." },
      ]}, }, duration: "15 min" },
      { id: "py-modules", title: "Modules aur Packages", description: "Python modules aur pip", order: 9, content: { introduction: "Modules Python code organize karne ka tarika hai. Standard library mein 200+ modules hain. Third-party packages pip se install karte hain.", sections: [
        { title: "Importing Modules", explanation: "import module. from module import function. import module as alias. from module import *. Standard library modules: os, sys, math, datetime, random, json, re, collections.", example: "import os; print(os.getcwd())" },
        { title: "pip aur Virtual Environment", explanation: "pip install package_name. pip list. pip freeze > requirements.txt. pip install -r requirements.txt. Virtual environments (venv) - project-specific dependencies. python -m venv .venv.", example: "pip install requests numpy pandas" },
      ], codeExamples: [
        { title: "Modules Example", language: "python", code: "# Standard library imports\nimport os\nimport sys\nimport math\nimport random\nimport datetime\nfrom collections import Counter, defaultdict\n\n# OS module\nprint('Current dir:', os.getcwd())\nprint('Files:', os.listdir('.'))\n\n# Math module\nprint('Pi:', math.pi)\nprint('Square root:', math.sqrt(16))\nprint('Random:', random.randint(1, 100))\n\n# Datetime\nnow = datetime.datetime.now()\nprint('Current time:', now)\nprint('Formatted:', now.strftime('%Y-%m-%d %H:%M:%S'))\n\n# Collections\nfreq = Counter(['a', 'b', 'a', 'c', 'b', 'a'])\nprint('Most common:', freq.most_common(2))\n\n# Create your own module\n# my_module.py: def greet(name): return f'Hello {name}'\n# In another file: from my_module import greet\n\n# Third-party packages (install first: pip install requests)\nimport requests\ntry:\n    response = requests.get('https://api.github.com')\n    print('GitHub API status:', response.status_code)\nexcept ImportError:\n    print('requests package installed nahi hai. pip install requests karein.')\n\n# requirements.txt\n# pip freeze > requirements.txt\n# pip install -r requirements.txt", output: "Standard library, third-party packages, pip usage." },
      ], notes: ["import module se import karte hain", "pip package manager hai", "virtual environments project isolation ke liye", "requirements.txt dependencies list karta hai"],
      commonMistakes: [{ mistake: "System-wide packages install karna (venv use na karna)", correction: "Har project ke liye alag virtual environment banayein" }],
      interviewQuestions: ["__name__ == '__main__' kya karta hai?", "pip kya hai?", "Virtual environment kyun use karte hain?"],
      practiceExercises: [{ question: "requests library use karke kisi public API se data fetch karein aur JSON parse karein.", difficulty: "medium" }],
      quiz: { id: "quiz-py-modules", title: "Modules Quiz", questions: [
        { id: "q1", question: "Packages install karne ke liye kaunsa command use hota hai?", options: ["python install", "pip install", "npm install", "install package"], correctAnswer: 1, explanation: "pip install se packages install hote hain." },
      ]}, }, duration: "15 min" },
      { id: "py-project", title: "Project - Weather App", description: "Python project banayein", order: 10, content: { introduction: "Ek CLI weather application banayenge jo API se weather data fetch karega, user input handle karega, aur formatted output display karega.", sections: [
        { title: "Project Overview", explanation: "Weather app jo city name input lekar us city ka current weather display kare. openweathermap API use karein. JSON parse karein. Error handling. User-friendly interface.", example: "Input: 'Delhi' -> Output: 'Temperature: 32°C, Humidity: 60%, Conditions: Clear Sky'" },
        { title: "Implementation", explanation: "requests library API call ke liye. JSON parsing. f-strings for output. try-except for error handling. while loop for continuous interaction. .env file for API key.", example: "Response structure: { 'main': { 'temp': 305, 'humidity': 60 }, 'weather': [{ 'description': 'clear sky' }] }" },
      ], codeExamples: [
        { title: "Weather App", language: "python", code: "import requests\nimport json\nimport os\nfrom dotenv import load_dotenv\n\nload_dotenv()\n\nclass WeatherApp:\n    def __init__(self):\n        self.api_key = os.getenv('WEATHER_API_KEY')\n        self.base_url = 'https://api.openweathermap.org/data/2.5/weather'\n    \n    def get_weather(self, city):\n        \"\"\"Fetch weather data for a city\"\"\"\n        try:\n            params = {\n                'q': city,\n                'appid': self.api_key,\n                'units': 'metric',  # Celsius\n                'lang': 'hi'        # Hindi (if supported)\n            }\n            \n            response = requests.get(self.base_url, params=params, timeout=5)\n            response.raise_for_status()\n            \n            return response.json()\n            \n        except requests.exceptions.HTTPError as e:\n            if response.status_code == 404:\n                raise ValueError(f\"City '{city}' nahi mila\")\n            elif response.status_code == 401:\n                raise PermissionError('Invalid API key')\n            else:\n                raise ConnectionError(f'API error: {e}')\n        except requests.exceptions.Timeout:\n            raise TimeoutError('Request timed out')\n        except requests.exceptions.ConnectionError:\n            raise ConnectionError('Internet connection nahi hai')\n    \n    def display_weather(self, data):\n        \"\"\"Display weather data in formatted way\"\"\"\n        city = data['name']\n        country = data['sys']['country']\n        temp = data['main']['temp']\n        feels_like = data['main']['feels_like']\n        humidity = data['main']['humidity']\n        pressure = data['main']['pressure']\n        description = data['weather'][0]['description']\n        wind_speed = data['wind']['speed']\n        \n        print('\\n' + '='*40)\n        print(f\"  {city}, {country}\")\n        print('='*40)\n        print(f\"  Weather: {description.title()}\")\n        print(f\"  Temperature: {temp:.1f}°C\")\n        print(f\"  Feels Like: {feels_like:.1f}°C\")\n        print(f\"  Humidity: {humidity}%\")\n        print(f\"  Pressure: {pressure} hPa\")\n        print(f\"  Wind Speed: {wind_speed} m/s\")\n        print('='*40 + '\\n')\n    \n    def run(self):\n        \"\"\"Main application loop\"\"\"\n        print('🌤️  Weather Application')\n        print('Type \"quit\" to exit\\n')\n        \n        while True:\n            try:\n                city = input('City name: ').strip()\n                \n                if city.lower() == 'quit':\n                    print('Goodbye!')\n                    break\n                \n                if not city:\n                    print('Please enter a city name')\n                    continue\n                \n                data = self.get_weather(city)\n                self.display_weather(data)\n                \n            except ValueError as e:\n                print(f'❌ {e}')\n            except ConnectionError as e:\n                print(f'❌ {e}')\n            except PermissionError as e:\n                print(f'❌ {e}')\n            except Exception as e:\n                print(f'❌ Unexpected error: {e}')\n\nif __name__ == '__main__':\n    app = WeatherApp()\n    app.run()\n\n# Requirements: requests python-dotenv\n# .env file: WEATHER_API_KEY=your_key_here\n# Run: python weather.py", output: "Complete weather application with error handling." },
      ], notes: ["API integration with requests library", "try-except for error handling", "OOP principles (class, methods)", "Environment variables for API keys"],
      commonMistakes: [{ mistake: "API key ko code mein hardcode karna", correction: "Environment variables mein store karein" }],
      interviewQuestions: ["API integration kaise karte hain?", "Error handling kaise kiya?", "Class-based design kyun choose kiya?"],
      practiceExercises: [{ question: "Weather app mein forecast feature add karein (5-day forecast API)", difficulty: "hard" }],
      quiz: { id: "quiz-py-project", title: "Python Project Quiz", questions: [
        { id: "q1", question: "API key store karne ka safe tarika kaunsa hai?", options: ["Hardcode in code", "Environment variables", "Text file", "Database"], correctAnswer: 1, explanation: "Environment variables safest hain." },
      ]}, }, duration: "30 min" },
    ],
  }],
}
