import { Course } from "@/types"

export const sqlCourse: Course = {
  id: "sql", title: "SQL",
  description: "SQL seekhein - Relational databases master karein",
  longDescription: "SQL (Structured Query Language) relational databases mein data store, query, aur manipulate karne ki standard language hai. Is course mein aap SELECT, JOIN, subqueries, indexes, normalization seekhenge.",
  category: "database", icon: "Database", color: "from-blue-700 to-indigo-700",
  difficulty: "intermediate", lessonsCount: 10, duration: "4 hours",
  prerequisites: ["Basic programming knowledge"],
  modules: [{
    id: "sql-core", title: "SQL Core", description: "Queries, joins, database design", order: 1, lessons: [
      { id: "sql-what-is", title: "SQL Kya Hai?", description: "SQL introduction", order: 1, content: { introduction: "SQL (Structured Query Language) relational databases ke saath communicate karne ki standard language hai. Isse data create, read, update, delete aur manage karte hain.", sections: [
        { title: "SQL Overview", explanation: "SQL 1970s mein IBM ne banaya. ANSI aur ISO standard hai. Relational databases (MySQL, PostgreSQL, SQL Server, SQLite) sab SQL use karte hain. DDL (Data Definition), DML (Data Manipulation), DCL (Data Control), TCL (Transaction Control).", example: "SELECT * FROM users WHERE age > 18;" },
        { title: "Database Basics", explanation: "Tables relational database mein data store karte hain. Columns (fields) define data type. Rows (records) actual data. Primary key unique identifies each row. Foreign key relates tables.", example: "users table: id (PK), name, email, age" },
      ], codeExamples: [
        { title: "Basic SQL", language: "sql", code: "-- Create database\nCREATE DATABASE mydb;\n\n-- Use database\nUSE mydb;\n\n-- Create table\nCREATE TABLE users (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(100) NOT NULL,\n    email VARCHAR(255) UNIQUE NOT NULL,\n    age INT CHECK (age >= 0),\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\n-- Insert data\nINSERT INTO users (name, email, age) VALUES ('Rahul', 'rahul@test.com', 25);\n\n-- Query data\nSELECT * FROM users;\nSELECT name, email FROM users WHERE age > 18 ORDER BY name ASC;", output: "SQL database operations - create, insert, select." },
      ], notes: ["SQL relational databases ki language hai", "DDL: CREATE, ALTER, DROP", "DML: SELECT, INSERT, UPDATE, DELETE", "Primary key unique identifier hai"],
      commonMistakes: [{ mistake: "String values mein single quotes bhoolna", correction: "SQL strings hamesha single quotes mein likhein" }],
      interviewQuestions: ["SQL kya hai?", "DDL aur DML mein kya antar hai?", "Primary key kya hai?"],
      practiceExercises: [{ question: "Database create karein, table banayein aur 5 records insert karein.", difficulty: "easy" }],
      quiz: { id: "quiz-sql-intro", title: "SQL Intro Quiz", questions: [
        { id: "q1", question: "SQL ka full form kya hai?", options: ["Simple Query Language", "Structured Query Language", "Standard Query Language", "System Query Language"], correctAnswer: 1, explanation: "SQL = Structured Query Language." },
      ]}, }, duration: "15 min" },
      { id: "sql-select", title: "SELECT Queries", description: "SELECT queries aur data filtering", order: 2, content: { introduction: "SELECT statement data query karne ke liye use hota hai. Ye SQL ka sabse common operation hai.", sections: [
        { title: "SELECT Basics", explanation: "SELECT columns FROM table. DISTINCT - unique values. WHERE - filtering. AS - alias. LIMIT - rows limit. OFFSET - skip rows. ORDER BY - sorting (ASC/DESC).", example: "SELECT DISTINCT city FROM users WHERE age > 18 ORDER BY city DESC LIMIT 10;" },
        { title: "WHERE Clause Operators", explanation: "=, !=, <>, >, <, >=, <=. BETWEEN - range. IN - list match. LIKE - pattern matching (% for any, _ for single). IS NULL - null check. AND, OR, NOT - logical.", example: "WHERE age BETWEEN 18 AND 30 AND name LIKE 'R%'" },
      ], codeExamples: [
        { title: "SELECT Queries", language: "sql", code: "-- Basic SELECT\nSELECT * FROM users;\nSELECT name, email FROM users;\n\n-- WHERE with different operators\nSELECT * FROM users WHERE age >= 18;\nSELECT * FROM users WHERE age BETWEEN 25 AND 35;\nSELECT * FROM users WHERE city IN ('Delhi', 'Mumbai');\nSELECT * FROM users WHERE name LIKE 'R%';\nSELECT * FROM users WHERE email IS NOT NULL;\n\n-- Sorting and limiting\nSELECT * FROM users ORDER BY age DESC, name ASC LIMIT 10 OFFSET 20;\n\n-- Aggregation functions\nSELECT \n    COUNT(*) AS total_users,\n    AVG(age) AS avg_age,\n    MAX(age) AS max_age,\n    MIN(age) AS min_age\nFROM users;\n\n-- Group By\nSELECT city, COUNT(*) as count, AVG(age) as avg_age\nFROM users\nGROUP BY city\nHAVING COUNT(*) > 5\nORDER BY count DESC;\n\n-- String functions\nSELECT UPPER(name), LENGTH(email), SUBSTRING(name, 1, 3) FROM users;\n\n-- Date functions\nSELECT YEAR(created_at), MONTH(created_at), COUNT(*)\nFROM users\nGROUP BY YEAR(created_at), MONTH(created_at);", output: "SELECT queries with WHERE, aggregation, GROUP BY, HAVING, functions." },
      ], notes: ["WHERE clause mein AND/OR combine karein", "LIKE '%text%' pattern matching", "GROUP BY aggregate functions ke saath", "HAVING GROUP BY ke baad filter ke liye"],
      commonMistakes: [{ mistake: "WHERE mein aggregate function use karna (HAVING use karein)", correction: "HAVING aggregate conditions ke liye, WHERE row-level ke liye" }],
      interviewQuestions: ["GROUP BY aur HAVING mein kya antar hai?", "WHERE aur HAVING mein difference?", "LIKE operator ka pattern kya hai?"],
      practiceExercises: [{ question: "E-commerce database mein 10 SELECT queries likhein - different conditions, sorting, aggregation, grouping.", difficulty: "medium" }],
      quiz: { id: "quiz-sql-select", title: "SELECT Quiz", questions: [
        { id: "q1", question: "Aggregate function ke saath GROUP BY ke baad filter ke liye kaunsa clause use hota hai?", options: ["WHERE", "HAVING", "FILTER", "CONDITION"], correctAnswer: 1, explanation: "HAVING GROUP BY ke baad filter karta hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-joins", title: "JOINs", description: "SQL JOINs - multiple tables connect karna", order: 3, content: { introduction: "JOINs multiple tables ko combine karte hain related columns ke through. Normalized databases mein data multiple tables mein divide hota hai aur JOINs unhe wapas combine karte hain.", sections: [
        { title: "Types of JOINs", explanation: "INNER JOIN - matching rows dono tables se. LEFT JOIN - all rows from left, matching from right. RIGHT JOIN - all rows from right, matching from left. FULL JOIN - all rows from both. CROSS JOIN - cartesian product.", example: "SELECT u.name, o.total FROM users u INNER JOIN orders o ON u.id = o.user_id" },
        { title: "Self JOIN aur Multiple JOINs", explanation: "Self JOIN - ek table ko apne saath join karna (jaise employees table mein manager_id). Multiple JOINs - 2+ tables join karna. Aliases zaroori hain for clarity.", example: "SELECT e.name, m.name FROM employees e LEFT JOIN employees m ON e.manager_id = m.id" },
      ], codeExamples: [
        { title: "JOIN Examples", language: "sql", code: "-- INNER JOIN\nSELECT u.name, o.id as order_id, o.total, o.created_at\nFROM users u\nINNER JOIN orders o ON u.id = o.user_id\nWHERE o.total > 500\nORDER BY o.created_at DESC;\n\n-- LEFT JOIN\nSELECT u.name, COUNT(o.id) as order_count, COALESCE(SUM(o.total), 0) as total_spent\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nGROUP BY u.id, u.name;\n\n-- Multiple JOINs\nSELECT o.id as order_id, u.name, p.name as product_name, oi.quantity\nFROM orders o\nJOIN users u ON o.user_id = u.id\nJOIN order_items oi ON o.id = oi.order_id\nJOIN products p ON oi.product_id = p.id\nWHERE o.status = 'delivered';\n\n-- Self JOIN (Employees and Managers)\nSELECT e.name as employee, m.name as manager\nFROM employees e\nLEFT JOIN employees m ON e.manager_id = m.id;\n\n-- FULL JOIN (emulated in MySQL - UNION of LEFT and RIGHT)\nSELECT u.name, o.id\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nUNION\nSELECT u.name, o.id\nFROM users u\nRIGHT JOIN orders o ON u.id = o.user_id;", output: "Different JOIN types with real-world examples." },
      ], notes: ["INNER JOIN sirf matching rows", "LEFT JOIN all from left", "Aliases (u, o) table names shorten karte hain", "Multiple JOINs possible hain"],
      commonMistakes: [{ mistake: "JOIN condition bhoolna (cross join ho jayega)", correction: "Hamesha ON clause mein join condition dena" }],
      interviewQuestions: ["INNER aur LEFT JOIN mein kya antar hai?", "Self JOIN kab use karte hain?", "Multiple JOINs ka order kya hota hai?"],
      practiceExercises: [{ question: "4-table JOIN query likhein - users, orders, order_items, products", difficulty: "hard" }],
      quiz: { id: "quiz-sql-joins", title: "JOINs Quiz", questions: [
        { id: "q1", question: "Left table ke saare rows aur right table ke matching rows kaunsa JOIN return karta hai?", options: ["INNER", "LEFT", "RIGHT", "FULL"], correctAnswer: 1, explanation: "LEFT JOIN left table ke saare rows return karta hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-subqueries", title: "Subqueries aur CTEs", description: "Subqueries aur Common Table Expressions", order: 4, content: { introduction: "Subqueries ek query ke andar doosri query hoti hai. CTEs (WITH clause) complex queries ko readable aur reusable banate hain.", sections: [
        { title: "Subqueries", explanation: "Scalar subquery - single value return. Row subquery - single row. Table subquery - multiple rows. EXISTS - existence check. IN/ NOT IN - list mein check. Correlated subquery - outer query reference karta hai.", example: "SELECT * FROM users WHERE id IN (SELECT user_id FROM orders WHERE total > 1000)" },
        { title: "CTE (Common Table Expression)", explanation: "WITH cte_name AS (query) SELECT FROM cte_name. Recursive CTE - self-referencing (hierarchical data). Multiple CTEs comma se separate. Better readability than nested subqueries.", example: "WITH high_spenders AS (SELECT user_id, SUM(total) FROM orders GROUP BY user_id HAVING SUM(total) > 5000) SELECT * FROM users JOIN high_spenders;" },
      ], codeExamples: [
        { title: "Subqueries and CTEs", language: "sql", code: "-- Scalar subquery in SELECT\nSELECT name, (SELECT COUNT(*) FROM orders WHERE user_id = users.id) as order_count\nFROM users;\n\n-- Subquery in WHERE with IN\nSELECT * FROM products\nWHERE category_id IN (\n    SELECT id FROM categories WHERE name LIKE '%electronics%'\n);\n\n-- EXISTS subquery\nSELECT u.* FROM users u\nWHERE EXISTS (\n    SELECT 1 FROM orders o \n    WHERE o.user_id = u.id AND o.total > 1000\n);\n\n-- Correlated subquery\nSELECT p.name, p.price\nFROM products p\nWHERE p.price > (\n    SELECT AVG(price) FROM products WHERE category_id = p.category_id\n);\n\n-- CTE (WITH clause)\nWITH category_stats AS (\n    SELECT \n        category_id,\n        COUNT(*) as product_count,\n        AVG(price) as avg_price\n    FROM products\n    GROUP BY category_id\n),\nhigh_value_categories AS (\n    SELECT * FROM category_stats WHERE avg_price > 500\n)\nSELECT c.name, h.*\nFROM high_value_categories h\nJOIN categories c ON h.category_id = c.id\nORDER BY h.avg_price DESC;\n\n-- Recursive CTE (hierarchy)\nWITH RECURSIVE org_chart AS (\n    -- Base case\n    SELECT id, name, manager_id, 1 as level\n    FROM employees WHERE manager_id IS NULL\n    UNION ALL\n    -- Recursive step\n    SELECT e.id, e.name, e.manager_id, oc.level + 1\n    FROM employees e\n    JOIN org_chart oc ON e.manager_id = oc.id\n)\nSELECT * FROM org_chart ORDER BY level, name;", output: "Subqueries, CTEs, aur recursive CTE examples." },
      ], notes: ["Subqueries SELECT, WHERE, FROM mein use ho sakti hain", "CTEs queries ko modular banate hain", "Recursive CTEs hierarchical data ke liye", "Correlated subquery row-by-row evaluate hoti hai"],
      commonMistakes: [{ mistake: "Correlated subquery ka performance impact na samajhna", correction: "Large datasets mein JOINs ya CTEs prefer karein" }],
      interviewQuestions: ["Subquery aur CTE mein kya antar hai?", "Correlated subquery kya hai?", "Recursive CTE kab use karein?"],
      practiceExercises: [{ question: "CTE ka use karke monthly sales report query banayein - total revenue, order count, avg order value.", difficulty: "hard" }],
      quiz: { id: "quiz-sql-subquery", title: "Subqueries Quiz", questions: [
        { id: "q1", question: "CTE define karne ke liye kaunsa keyword use hota hai?", options: ["CTE", "WITH", "TEMP", "DEFINE"], correctAnswer: 1, explanation: "WITH clause CTE define karta hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-normalization", title: "Database Normalization", description: "Normalization aur database design", order: 5, content: { introduction: "Normalization data redundancy reduce aur data integrity improve karne ka process hai. Normal forms (NF) ensure karte hain ki database efficiently structured hai.", sections: [
        { title: "Normal Forms", explanation: "1NF - atomic values (no repeating groups). 2NF - 1NF + partial dependency remove (all non-key columns depend on full primary key). 3NF - 2NF + transitive dependency remove (no indirect dependencies). BCNF - stricter 3NF.", example: "1NF: Single value per cell. 2NF: Single column primary key. 3NF: No column depends on non-key column." },
        { title: "Denormalization", explanation: "Denormalization performance ke liye intentionally redundancy add karna. Reporting/OLAP systems mein common. Trade-off: storage + complexity vs read performance. Materialized views, summary tables.", example: "orders table mein user_name dena (avoid join on read)" },
      ], codeExamples: [
        { title: "Normalization Example", language: "sql", code: "-- UNNORMALIZED (problems: duplicate data, update anomaly)\nCREATE TABLE orders_unnormalized (\n    order_id INT,\n    customer_name VARCHAR(100),\n    customer_email VARCHAR(100),\n    product_name VARCHAR(100),\n    product_price DECIMAL,\n    quantity INT\n);\n\n-- 1NF: Atomic values, primary key\nCREATE TABLE orders_1nf (\n    order_id INT,\n    customer_name VARCHAR(100),\n    customer_email VARCHAR(100),\n    product_name VARCHAR(100),\n    product_price DECIMAL,\n    quantity INT,\n    PRIMARY KEY (order_id, product_name)\n);\n\n-- 2NF: Remove partial dependencies (separate products table)\nCREATE TABLE orders_2nf (\n    order_id INT PRIMARY KEY,\n    customer_name VARCHAR(100),\n    customer_email VARCHAR(100),\n    -- product info removed\n);\n\nCREATE TABLE order_items (\n    order_id INT,\n    product_name VARCHAR(100),\n    quantity INT,\n    PRIMARY KEY (order_id, product_name)\n);\n\n-- 3NF: Remove transitive dependencies (separate customers table)\nCREATE TABLE customers (\n    customer_id INT PRIMARY KEY,\n    name VARCHAR(100),\n    email VARCHAR(100)\n);\n\nCREATE TABLE orders (\n    order_id INT PRIMARY KEY,\n    customer_id INT,\n    order_date DATE,\n    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)\n);\n\nCREATE TABLE products (\n    product_id INT PRIMARY KEY,\n    name VARCHAR(100),\n    price DECIMAL\n);\n\nCREATE TABLE order_items (\n    order_id INT,\n    product_id INT,\n    quantity INT,\n    PRIMARY KEY (order_id, product_id),\n    FOREIGN KEY (order_id) REFERENCES orders(order_id),\n    FOREIGN KEY (product_id) REFERENCES products(product_id)\n);", output: "Unnormalized to 3NF database design evolution." },
      ], notes: ["1NF: Atomic values, no repeating groups", "2NF: 1NF + full functional dependency", "3NF: 2NF + no transitive dependency", "Normalization data integrity increase karta hai"],
      commonMistakes: [{ mistake: "Over-normalization (too many tables, complex queries)", correction: "Performance requirements ke according normalize karein" }],
      interviewQuestions: ["Normalization kya hai?", "1NF, 2NF, 3NF mein kya antar hai?", "Denormalization kab use karein?"],
      practiceExercises: [{ question: "Unnormalized e-commerce schema normalize karke 3NF mein design karein.", difficulty: "hard" }],
      quiz: { id: "quiz-sql-normalization", title: "Normalization Quiz", questions: [
        { id: "q1", question: "Kaunsa normal form transitive dependency remove karta hai?", options: ["1NF", "2NF", "3NF", "BCNF"], correctAnswer: 2, explanation: "3NF transitive dependency remove karta hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-indexes", title: "Indexes aur Performance", description: "SQL indexes aur query optimization", order: 6, content: { introduction: "Indexes database mein query performance improve karne ke liye use hote hain. Like book ki index - jaldi search karna. But indexes write performance degrade karte hain.", sections: [
        { title: "Index Types", explanation: "Primary key - clustered index (data physically ordered). Unique index - duplicate prevention. Composite index - multiple columns. Full-text index - text search. Partial index - conditional. Covering index - includes all queried columns.", example: "CREATE INDEX idx_users_email ON users(email);" },
        { title: "Index Best Practices", explanation: "Frequently queried columns par index. WHERE, JOIN, ORDER BY mein use hone wale columns. Composite index column order matters - most selective first. Indexing overhead understand karein. EXPLAIN ANALYZE se query plan check.", example: "EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'test@test.com';" },
      ], codeExamples: [
        { title: "Index Management", language: "sql", code: "-- Create indexes\nCREATE INDEX idx_users_email ON users(email);\nCREATE UNIQUE INDEX idx_users_email_unique ON users(email);\n\n-- Composite index (column order matters!)\nCREATE INDEX idx_orders_user_status ON orders(user_id, status);\n\n-- Partial index (PostgreSQL)\nCREATE INDEX idx_active_users ON users(created_at) WHERE status = 'active';\n\n-- Full-text index\nCREATE INDEX idx_products_search ON products USING GIN(to_tsvector('english', name || ' ' || description));\n\n-- Drop index\nDROP INDEX idx_users_email;\n\n-- Check indexes\nSELECT * FROM pg_indexes WHERE tablename = 'users';\n\n-- Query optimization with EXPLAIN\nEXPLAIN ANALYZE\nSELECT u.name, COUNT(o.id) as orders\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nWHERE u.created_at > '2024-01-01'\nGROUP BY u.id, u.name\nORDER BY orders DESC\nLIMIT 10;\n\n-- Use indexes for\n-- 1. WHERE conditions\n-- 2. JOIN conditions \n-- 3. ORDER BY sorting\n-- 4. GROUP BY grouping\n-- Avoid functions on indexed columns: WHERE DATE(created_at) = '2024-01-01' -- bad!\n-- Better: WHERE created_at >= '2024-01-01' AND created_at < '2024-01-02'", output: "Index creation, management, aur query optimization tips." },
      ], notes: ["Indexes read performance improve karte hain", "Indexes write performance degrade karte hain", "Composite index column order important hai", "EXPLAIN ANALYZE se query optimization"],
      commonMistakes: [{ mistake: "Har column par index banana (write performance degrade)", correction: "Sirf frequently queried columns par index banayein" }],
      interviewQuestions: ["Index kya hai?", "Clustered aur non-clustered index mein kya antar hai?", "Composite index mein column order kyun important hai?"],
      practiceExercises: [{ question: "EXPLAIN ANALYZE se query plan check karein aur appropriate indexes create karein for slow queries.", difficulty: "medium" }],
      quiz: { id: "quiz-sql-indexes", title: "Indexes Quiz", questions: [
        { id: "q1", question: "Indexed column par function use karne se index use hota hai?", options: ["Haan", "Nahi", "Kabhi kabhi", "Sirf date functions"], correctAnswer: 1, explanation: "Function indexed column par index use nahi hota." },
      ]}, }, duration: "15 min" },
      { id: "sql-transactions", title: "Transactions aur Concurrency", description: "SQL transactions aur ACID properties", order: 7, content: { introduction: "Transactions multiple database operations ko ek unit mein group karte hain. ACID properties ensure karte hain ki transactions reliable hain.", sections: [
        { title: "ACID Properties", explanation: "Atomicity - all or nothing. Consistency - valid state maintain. Isolation - transactions interfere nahi karte. Durability - committed data remains. BEGIN, COMMIT, ROLLBACK.", example: "BEGIN; UPDATE accounts SET balance = balance - 100; UPDATE accounts SET balance = balance + 100; COMMIT;" },
        { title: "Isolation Levels", explanation: "READ UNCOMMITTED - dirty reads. READ COMMITTED - no dirty reads (default in PostgreSQL). REPEATABLE READ - consistent snapshot. SERIALIZABLE - highest isolation. Higher level = more protection, less concurrency.", example: "SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;" },
      ], codeExamples: [
        { title: "Transactions Example", language: "sql", code: "-- Basic transaction\nBEGIN;\n\nUPDATE accounts \nSET balance = balance - 500 \nWHERE account_id = 1 AND balance >= 500;\n\nIF NOT FOUND THEN\n    ROLLBACK;\n    -- Insufficient balance\nEND IF;\n\nUPDATE accounts \nSET balance = balance + 500 \nWHERE account_id = 2;\n\nCOMMIT;\n\n-- Savepoint (partial rollback)\nBEGIN;\nINSERT INTO orders (user_id, total) VALUES (1, 1000);\n\nSAVEPOINT order_created;\n\nINSERT INTO order_items (order_id, product_id, quantity) \nVALUES (LASTVAL(), 1, 2);\n\n-- If something wrong with items, rollback to savepoint\nROLLBACK TO SAVEPOINT order_created;\n\n-- Try again with different items\nINSERT INTO order_items (order_id, product_id, quantity) \nVALUES (LASTVAL(), 2, 5);\n\nCOMMIT;\n\n-- Optimistic locking (using version column)\nUPDATE products \nSET stock = stock - 1, version = version + 1\nWHERE id = 10 AND version = 5;\n-- If 0 rows affected, someone else already updated\n\n-- Deadlock example (avoid!)\n-- Transaction 1: UPDATE A -> UPDATE B\n-- Transaction 2: UPDATE B -> UPDATE A\n-- Solution: Always update in same order!", output: "Transactions with BEGIN/COMMIT/ROLLBACK, savepoints, locking." },
      ], notes: ["BEGIN se transaction start, COMMIT se save, ROLLBACK se undo", "Savepoints partial rollback ke liye", "Isolation level performance aur consistency balance karta hai", "Deadlock se bachne ke liye consistent update order"],
      commonMistakes: [{ mistake: "Transaction mein error handling na karna", correction: "Try-catch aur ROLLBACK implement karein" }],
      interviewQuestions: ["ACID kya hai?", "Isolation levels kaun se hain?", "Dirty read kya hai?"],
      practiceExercises: [{ question: "Bank transfer transaction implement karein with proper error handling aur rollback.", difficulty: "medium" }],
      quiz: { id: "quiz-sql-tx", title: "Transactions Quiz", questions: [
        { id: "q1", question: "Transaction permanently save karne ke liye kaunsa command use hota hai?", options: ["SAVE", "COMMIT", "END", "FLUSH"], correctAnswer: 1, explanation: "COMMIT transaction save karta hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-procedures", title: "Stored Procedures aur Functions", description: "Stored procedures aur database functions", order: 8, content: { introduction: "Stored procedures aur functions database-side logic hain. Ye SQL code ko encapsulate karte hain, reuse karte hain aur performance improve karte hain (network round trips kam).", sections: [
        { title: "Stored Procedures", explanation: "CREATE PROCEDURE se bante hain. IN/OUT/INOUT parameters. Variables declare (DECLARE). Control flow (IF, WHILE). Transaction management. CALL procedure_name() se execute.", example: "CREATE PROCEDURE GetUserOrders(IN userId INT) BEGIN SELECT * FROM orders WHERE user_id = userId; END;" },
        { title: "Functions vs Procedures", explanation: "Functions RETURN value dete hain. SELECT mein use ho sakte hain. Procedures OUT parameters ya result sets return karte hain. Functions call from SELECT, Procedures call separately.", example: "CREATE FUNCTION get_user_email(userId INT) RETURNS VARCHAR(255) BEGIN RETURN (SELECT email FROM users WHERE id = userId); END;" },
      ], codeExamples: [
        { title: "Stored Procedures", language: "sql", code: "-- Stored Procedure\nCREATE PROCEDURE TransferMoney(\n    IN from_account INT,\n    IN to_account INT,\n    IN amount DECIMAL(10,2),\n    OUT status VARCHAR(100)\n)\nBEGIN\n    DECLARE current_balance DECIMAL(10,2);\n    \n    START TRANSACTION;\n    \n    SELECT balance INTO current_balance \n    FROM accounts WHERE id = from_account FOR UPDATE;\n    \n    IF current_balance >= amount THEN\n        UPDATE accounts SET balance = balance - amount WHERE id = from_account;\n        UPDATE accounts SET balance = balance + amount WHERE id = to_account;\n        SET status = 'Success';\n        COMMIT;\n    ELSE\n        SET status = 'Insufficient balance';\n        ROLLBACK;\n    END IF;\nEND;\n\n-- Call procedure\nCALL TransferMoney(1, 2, 500, @status);\nSELECT @status;\n\n-- Function\nCREATE FUNCTION CalculateOrderTotal(orderId INT)\nRETURNS DECIMAL(10,2)\nDETERMINISTIC\nBEGIN\n    DECLARE total DECIMAL(10,2);\n    \n    SELECT SUM(oi.quantity * oi.price) INTO total\n    FROM order_items oi\n    WHERE oi.order_id = orderId;\n    \n    RETURN COALESCE(total, 0);\nEND;\n\n-- Use function\nSELECT id, CalculateOrderTotal(id) as total FROM orders LIMIT 10;\n\n-- Trigger\nCREATE TRIGGER after_order_insert\nAFTER INSERT ON orders\nFOR EACH ROW\nBEGIN\n    UPDATE users \n    SET order_count = order_count + 1\n    WHERE id = NEW.user_id;\nEND;", output: "Stored procedures, functions, aur triggers." },
      ], notes: ["Procedures multiple SQL statements group karte hain", "Functions value return karte hain", "Triggers automatic execute hote hain events par", "Variables DECLARE se declare karte hain"],
      commonMistakes: [{ mistake: "Stored procedures mein error handling na karna", correction: "DECLARE ... HANDLER for error handling use karein" }],
      interviewQuestions: ["Procedure aur function mein kya antar hai?", "Trigger kya hai?", "Variables kaise declare karte hain?"],
      practiceExercises: [{ question: "Stored procedure banaye for order processing with inventory validation.", difficulty: "hard" }],
      quiz: { id: "quiz-sql-procs", title: "Stored Procedures Quiz", questions: [
        { id: "q1", question: "Stored procedure execute karne ke liye kaunsa command use hota hai?", options: ["EXEC", "CALL", "RUN", "DO"], correctAnswer: 1, explanation: "CALL se procedure execute hota hai." },
      ]}, }, duration: "20 min" },
      { id: "sql-views", title: "Views aur Materialized Views", description: "Database views aur their benefits", order: 9, content: { introduction: "Views saved SQL queries hain jinhe table ki tarah use kar sakte hain. Ye security, simplicity aur abstraction provide karte hain.", sections: [
        { title: "Views", explanation: "CREATE VIEW view_name AS SELECT ... Simple view - single table, updatable. Complex view - multiple tables, read-only. Security - sensitive columns hide karna. Simplicity - complex queries simplify.", example: "CREATE VIEW active_users AS SELECT * FROM users WHERE status = 'active';" },
        { title: "Materialized Views", explanation: "Materialized views physically data store karte hain. Faster reads (pre-computed). Need REFRESH for updates. Reporting, data warehouse ke liye ideal. PostgreSQL supports natively.", example: "CREATE MATERIALIZED VIEW monthly_sales AS SELECT ... WITH DATA; REFRESH MATERIALIZED VIEW monthly_sales;" },
      ], codeExamples: [
        { title: "Views Example", language: "sql", code: "-- Simple view\nCREATE VIEW user_orders AS\nSELECT u.id, u.name, u.email, COUNT(o.id) as order_count, COALESCE(SUM(o.total), 0) as total_spent\nFROM users u\nLEFT JOIN orders o ON u.id = o.user_id\nGROUP BY u.id, u.name, u.email;\n\n-- Use view\nSELECT * FROM user_orders WHERE total_spent > 1000 ORDER BY total_spent DESC;\n\n-- View with security (hide sensitive columns)\nCREATE VIEW public_users AS\nSELECT id, name, email, created_at\nFROM users;\n-- password, ssn etc hidden\n\n-- Materialized View (PostgreSQL)\nCREATE MATERIALIZED VIEW daily_sales_report AS\nSELECT \n    DATE(o.created_at) as sale_date,\n    COUNT(DISTINCT o.id) as order_count,\n    COUNT(DISTINCT o.user_id) as unique_customers,\n    SUM(o.total) as total_revenue,\n    AVG(o.total) as avg_order_value\nFROM orders o\nWHERE o.status = 'delivered'\nGROUP BY DATE(o.created_at)\nORDER BY sale_date DESC;\n\n-- Refresh materialized view\nREFRESH MATERIALIZED VIEW daily_sales_report;\n\n-- Check if view is materialized\nSELECT * FROM daily_sales_report WHERE sale_date >= CURRENT_DATE - INTERVAL '7 days';", output: "Regular views aur materialized views with refresh." },
      ], notes: ["Views complex queries simplify karte hain", "Materialized views pre-computed data store karte hain", "Views security provide karte hain (column masking)", "Materialized views need manual refresh"],
      commonMistakes: [{ mistake: "Materialized view ko regular view samajhna (stale data expect karna)", correction: "Materialized views ka REFRESH schedule karein" }],
      interviewQuestions: ["View kya hai?", "View aur materialized view mein kya antar hai?", "Views ke kya fayde hain?"],
      practiceExercises: [{ question: "Complex reporting query se materialized view create karein aur refresh schedule setup karein.", difficulty: "medium" }],
      quiz: { id: "quiz-sql-views", title: "Views Quiz", questions: [
        { id: "q1", question: "Materialized view refresh karne ke liye kaunsa command use hota hai?", options: ["REFRESH", "UPDATE", "ALTER", "REBUILD"], correctAnswer: 0, explanation: "REFRESH MATERIALIZED VIEW command use hota hai." },
      ]}, }, duration: "15 min" },
      { id: "sql-project", title: "Project - E-commerce Database", description: "Complete e-commerce database banayein", order: 10, content: { introduction: "Ek complete e-commerce database design karenge jisme sab SQL concepts ka use hoga - normalization, joins, indexes, procedures, views, transactions.", sections: [
        { title: "Database Schema", explanation: "Tables: users, products, categories, orders, order_items, reviews, addresses. Relationships: users-orders (1:M), products-order_items (1:M), categories-products (1:M), users-reviews (1:M), products-reviews (1:M).", example: "Schema with proper primary keys, foreign keys, indexes, constraints" },
        { title: "Implementation", explanation: "Normalized schema (3NF). Indexes on foreign keys and frequently queried columns. Stored procedures for order processing. Views for reporting. Transactions for orders. Triggers for stock updates.", example: "Stored procedure: place_order, cancel_order. View: monthly_sales, top_products." },
      ], codeExamples: [
        { title: "E-commerce Database", language: "sql", code: "-- Complete e-commerce schema\n\nCREATE TABLE categories (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(100) NOT NULL,\n    slug VARCHAR(100) UNIQUE NOT NULL,\n    description TEXT,\n    parent_id INT,\n    FOREIGN KEY (parent_id) REFERENCES categories(id)\n);\n\nCREATE TABLE products (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(200) NOT NULL,\n    slug VARCHAR(200) UNIQUE NOT NULL,\n    description TEXT,\n    price DECIMAL(10,2) NOT NULL CHECK (price >= 0),\n    category_id INT NOT NULL,\n    stock INT NOT NULL DEFAULT 0 CHECK (stock >= 0),\n    rating DECIMAL(2,1) DEFAULT 0,\n    num_reviews INT DEFAULT 0,\n    is_active BOOLEAN DEFAULT TRUE,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (category_id) REFERENCES categories(id)\n);\n\nCREATE TABLE users (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    name VARCHAR(100) NOT NULL,\n    email VARCHAR(255) UNIQUE NOT NULL,\n    password_hash VARCHAR(255) NOT NULL,\n    phone VARCHAR(20),\n    role ENUM('customer', 'admin') DEFAULT 'customer',\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n\nCREATE TABLE addresses (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    user_id INT NOT NULL,\n    street VARCHAR(255),\n    city VARCHAR(100),\n    state VARCHAR(100),\n    pincode VARCHAR(10),\n    is_default BOOLEAN DEFAULT FALSE,\n    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n);\n\nCREATE TABLE orders (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    user_id INT NOT NULL,\n    total DECIMAL(10,2) NOT NULL,\n    status ENUM('pending', 'confirmed', 'shipped', 'delivered', 'cancelled') DEFAULT 'pending',\n    shipping_address_id INT,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (user_id) REFERENCES users(id),\n    FOREIGN KEY (shipping_address_id) REFERENCES addresses(id)\n);\n\nCREATE TABLE order_items (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    order_id INT NOT NULL,\n    product_id INT NOT NULL,\n    quantity INT NOT NULL CHECK (quantity > 0),\n    unit_price DECIMAL(10,2) NOT NULL,\n    FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE,\n    FOREIGN KEY (product_id) REFERENCES products(id)\n);\n\nCREATE TABLE reviews (\n    id INT PRIMARY KEY AUTO_INCREMENT,\n    user_id INT NOT NULL,\n    product_id INT NOT NULL,\n    rating INT NOT NULL CHECK (rating BETWEEN 1 AND 5),\n    comment TEXT,\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n    FOREIGN KEY (user_id) REFERENCES users(id),\n    FOREIGN KEY (product_id) REFERENCES products(id),\n    UNIQUE (user_id, product_id) -- one review per user per product\n);\n\n-- Indexes\nCREATE INDEX idx_products_category ON products(category_id);\nCREATE INDEX idx_orders_user ON orders(user_id);\nCREATE INDEX idx_orders_status ON orders(status);\nCREATE INDEX idx_order_items_order ON order_items(order_id);\nCREATE INDEX idx_order_items_product ON order_items(product_id);\nCREATE INDEX idx_reviews_product ON reviews(product_id);\n\n-- Order processing procedure\nCREATE PROCEDURE place_order(\n    IN p_user_id INT,\n    IN p_address_id INT,\n    IN p_items JSON,\n    OUT p_order_id INT\n)\nBEGIN\n    DECLARE total DECIMAL(10,2) DEFAULT 0;\n    DECLARE product_id INT;\n    DECLARE quantity INT;\n    DECLARE price DECIMAL(10,2);\n    DECLARE i INT DEFAULT 0;\n    DECLARE item_count INT;\n    \n    START TRANSACTION;\n    \n    SET item_count = JSON_LENGTH(p_items);\n    \n    -- Calculate total\n    WHILE i < item_count DO\n        SET product_id = JSON_UNQUOTE(JSON_EXTRACT(p_items, CONCAT('$[', i, '].product_id')));\n        SET quantity = JSON_UNQUOTE(JSON_EXTRACT(p_items, CONCAT('$[', i, '].quantity')));\n        \n        SELECT price INTO price FROM products WHERE id = product_id;\n        SET total = total + (price * quantity);\n        SET i = i + 1;\n    END WHILE;\n    \n    -- Create order\n    INSERT INTO orders (user_id, total, shipping_address_id)\n    VALUES (p_user_id, total, p_address_id);\n    \n    SET p_order_id = LAST_INSERT_ID();\n    \n    -- Add order items and update stock\n    SET i = 0;\n    WHILE i < item_count DO\n        SET product_id = JSON_UNQUOTE(JSON_EXTRACT(p_items, CONCAT('$[', i, '].product_id')));\n        SET quantity = JSON_UNQUOTE(JSON_EXTRACT(p_items, CONCAT('$[', i, '].quantity')));\n        \n        SELECT price INTO price FROM products WHERE id = product_id;\n        \n        INSERT INTO order_items (order_id, product_id, quantity, unit_price)\n        VALUES (p_order_id, product_id, quantity, price);\n        \n        UPDATE products SET stock = stock - quantity WHERE id = product_id;\n        \n        SET i = i + 1;\n    END WHILE;\n    \n    COMMIT;\nEND;\n\n-- Sales report view\nCREATE VIEW monthly_sales AS\nSELECT \n    DATE_FORMAT(o.created_at, '%Y-%m') as month,\n    COUNT(DISTINCT o.id) as order_count,\n    COUNT(DISTINCT o.user_id) as customer_count,\n    SUM(o.total) as revenue,\n    AVG(o.total) as avg_order_value\nFROM orders o\nWHERE o.status = 'delivered'\nGROUP BY DATE_FORMAT(o.created_at, '%Y-%m')\nORDER BY month DESC;", output: "Complete e-commerce database with tables, indexes, procedures, views." },
      ], notes: ["3NF normalized schema", "JSON stored procedure for order processing", "Transaction ensures data consistency", "Views for reporting simplicity"],
      commonMistakes: [{ mistake: "Stock validation na karna order processing mein", correction: "Stock check karein aur bacha hua stock decrement karein" }],
      interviewQuestions: ["E-commerce database kaise design karein?", "Order processing transaction mein kya steps hain?", "Inventory management kaise implement karein?"],
      practiceExercises: [{ question: "E-commerce database mein cancel_order procedure add karein jo stock restore kare aur order status update kare.", difficulty: "hard" }],
      quiz: { id: "quiz-sql-project", title: "SQL Project Quiz", questions: [
        { id: "q1", question: "Order processing mein consistency ke liye kaunsa concept use hota hai?", options: ["Index", "View", "Transaction", "Trigger"], correctAnswer: 2, explanation: "Transactions ensure data consistency." },
      ]}, }, duration: "30 min" },
    ],
  }],
}
