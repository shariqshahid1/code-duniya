import { Course } from "@/types"

export const nodejsCourse: Course = {
  id: "nodejs", title: "Node.js",
  description: "Node.js seekhein - Server-side JavaScript master karein",
  longDescription: "Node.js ek JavaScript runtime hai jo server-side programming enable karta hai. Is course mein aap modules, file system, streams, npm, Express integration seekhenge.",
  category: "backend", icon: "Server", color: "from-green-700 to-green-500",
  difficulty: "intermediate", lessonsCount: 10, duration: "4 hours",
  prerequisites: ["JavaScript ka knowledge"],
  modules: [{
    id: "nodejs-core", title: "Node.js Core", description: "Node fundamentals aur built-in modules", order: 1, lessons: [
      { id: "node-what-is", title: "Node.js Kya Hai?", description: "Node.js introduction", order: 1, content: { introduction: "Node.js ek JavaScript runtime hai jo Chrome ke V8 engine par based hai. Ye server-side aur back-end development ke liye use hota hai.", sections: [
        { title: "Node.js Kya Hai?", explanation: "Node.js Ryan Dahl ne 2009 mein banaya. Ye event-driven, non-blocking I/O model use karta hai. JavaScript ko browser ke bahar run karne ka platform hai. NPM (Node Package Manager) package ecosystem provide karta hai.", example: "Node.js = V8 Engine + I/O APIs + Event Loop + NPM" },
      ], codeExamples: [
        { title: "Pehla Node.js Program", language: "javascript", code: "// hello.js\nconsole.log('Hello from Node.js!');\n\n// Terminal mein run karein: node hello.js\n\n// HTTP Server\nconst http = require('http');\nconst server = http.createServer((req, res) => {\n    res.end('Namaste Duniya!');\n});\nserver.listen(3000, () => console.log('Server running on port 3000'));", output: "Node.js HTTP server console pring karega aur browser par response dega." },
      ], notes: ["Node.js JavaScript runtime hai", "Event-driven aur non-blocking hai", "npm package manager use hota hai", "Server-side development ke liye"],
      commonMistakes: [{ mistake: "Node.js ko browser JS samajhna", correction: "Node mein DOM nahi hai, browser APIs nahi hain" }],
      interviewQuestions: ["Node.js kya hai?", "Event loop kya hai?", "Non-blocking I/O kya hai?"],
      practiceExercises: [{ question: "Pehla Node.js server banaye jo port 3000 par listen kare aur 'Hello World' response kare.", difficulty: "easy" }],
      quiz: { id: "quiz-node-intro", title: "Node.js Intro Quiz", questions: [
        { id: "q1", question: "Node.js kinte engine par based hai?", options: ["SpiderMonkey", "V8", "JavaScriptCore", "Chakra"], correctAnswer: 1, explanation: "Node.js Chrome V8 engine par based hai." },
        { id: "q2", question: "Node.js package manager kaunsa hai?", options: ["pip", "npm", "yarn", "both npm and yarn"], correctAnswer: 3, explanation: "npm default hai, yarn alternative." },
      ]}, }, duration: "15 min" },
      { id: "node-modules", title: "Modules aur Require", description: "Node.js module system", order: 2, content: { introduction: "Node.js module system code organize karne ka tarika hai. CommonJS (require) aur ES modules (import) dono support karte hain.", sections: [
        { title: "CommonJS Module System", explanation: "module.exports se export, require() se import. Core modules (http, fs, path) bina path ke import hote hain. Local modules './' se, third-party modules bina './' se.", example: "const fs = require('fs'); const myModule = require('./myModule');" },
        { title: "ES Modules", explanation: "ES modules 'type: module' package.json mein se enable hote hain. import/export syntax use karta hai. Default aur named exports support.", example: "import { readFile } from 'fs'; export default function() {}" },
        { title: "Core Modules", explanation: "http - HTTP server. fs - file system operations. path - file paths handle. os - OS info. events - EventEmitter. buffer - binary data. stream - streaming data.", example: "const os = require('os'); console.log(os.cpus());" },
      ], codeExamples: [
        { title: "Modules Example", language: "javascript", code: "// math.js (CommonJS)\nfunction add(a, b) { return a + b; }\nfunction multiply(a, b) { return a * b; }\nmodule.exports = { add, multiply };\n\n// app.js\nconst math = require('./math');\nconsole.log(math.add(5, 3)); // 8\n\n// ES module (package.json mein \"type\": \"module\")\n// math.mjs\nexport const add = (a, b) => a + b;\n// app.mjs\nimport { add } from './math.mjs';", output: "Node.js module system ka demonstration." },
      ], notes: ["require() CommonJS mein use hota hai", "import/export ES modules mein", "Core modules bina path ke available hain", "Node 14+ dono systems support karta hai"],
      commonMistakes: [{ mistake: "ES module file mein require() use karna", correction: "Either CommonJS use karein ya ES modules, mix na karein" }],
      interviewQuestions: ["CommonJS aur ES modules mein kya antar hai?", "Core modules kaun se hain?", "package.json type field kya karta hai?"],
      practiceExercises: [{ question: "Ek module banaye jisme utility functions hon aur use require/import se use karein.", difficulty: "easy" }],
      quiz: { id: "quiz-node-modules", title: "Modules Quiz", questions: [
        { id: "q1", question: "CommonJS module export kaunsa object use karta hai?", options: ["export", "module.exports", "exports", "this"], correctAnswer: 1, explanation: "module.exports se exports karte hain." },
        { id: "q2", question: "Core module require karne ke liye path dena zaroori hai?", options: ["Haan", "Nahi", "Kabhi kabhi", "Sirf npm packages ke liye"], correctAnswer: 1, explanation: "Core modules bina path ke require hote hain." },
      ]}, }, duration: "15 min" },
      { id: "node-fs", title: "File System Module", description: "fs module se files handle karna", order: 3, content: { introduction: "fs module file system operations ke liye use hota hai. Read/write files, create/delete directories, file stats, file watching.", sections: [
        { title: "File Reading", explanation: "readFileSync() - synchronous (blocking). readFile() - asynchronous (callback). promises API - fs.promises.readFile(). encode utf-8 na den to buffer return karta hai.", example: "const data = fs.readFileSync('file.txt', 'utf-8');" },
        { title: "File Writing", explanation: "writeFileSync() - write. appendFileSync() - append. Unlink() - delete. mkdir() - directory create. rmdir() - directory delete (empty). rm() - recursive delete.", example: "fs.writeFileSync('output.txt', 'Hello World');" },
        { title: "Path Module", explanation: "path.join() - path segments join. path.resolve() - absolute path. path.basename() - file name. path.dirname() - directory name. path.extname() - file extension.", example: "path.join('folder', 'sub', 'file.txt');" },
      ], codeExamples: [
        { title: "File System Operations", language: "javascript", code: "const fs = require('fs');\nconst path = require('path');\n\n// Read file (async)\nfs.readFile('data.json', 'utf-8', (err, data) => {\n    if (err) {\n        console.error('Error:', err.message);\n        return;\n    }\n    console.log('File content:', data);\n});\n\n// Write file\nfs.writeFileSync('output.txt', 'Namaste Node.js!');\n\n// Directory operations\nif (!fs.existsSync('logs')) {\n    fs.mkdirSync('logs');\n}\n\n// File info\nconst stats = fs.statSync('output.txt');\nconsole.log('Size:', stats.size);\nconsole.log('Is File:', stats.isFile());\n\n// Path operations\nconst fullPath = path.join(__dirname, 'logs', 'app.log');\nconsole.log('Full path:', fullPath);\nconsole.log('Extension:', path.extname('image.jpg'));", output: "File read, write, directory operations aur path handling." },
      ], notes: ["Synchronous methods blocking hote hain", "Async methods better performance dete hain", "fs.promises API promises return karta hai", "Path module cross-platform path handling"],
      commonMistakes: [{ mistake: "Synchronous methods ka production mein use", correction: "Production mein async methods prefer karein" }],
      interviewQuestions: ["Sync aur async file methods mein kya antar hai?", "path.join aur path.resolve mein kya difference hai?", "__dirname aur __filename kya hain?"],
      practiceExercises: [{ question: "Ek program likhein jo file read kare, content modify kare aur new file mein save kare.", difficulty: "medium" }],
      quiz: { id: "quiz-node-fs", title: "File System Quiz", questions: [
        { id: "q1", question: "Async file read ke liye encoding parameter kya hota hai?", options: ["encoding", "utf-8", "charset", "format"], correctAnswer: 1, explanation: "'utf-8' encoding parameter hai." },
      ]}, }, duration: "20 min" },
      { id: "node-events", title: "Events aur EventEmitter", description: "Node.js event system", order: 4, content: { introduction: "Node.js event-driven architecture par based hai. Events module EventEmitter class provide karta hai jo custom events implement karne ke liye use hota hai.", sections: [
        { title: "EventEmitter", explanation: "on()/addListener() - event listen. emit() - event trigger. once() - ek baar listen. removeListener() - listener remove. Listener count unlimited by default (maxListeners).", example: "const EventEmitter = require('events'); const emitter = new EventEmitter();" },
        { title: "Event Patterns", explanation: "Error events always emit with error object. Return values ignored in events. Arguments as object prefer karein. Event names should be strings. Emit synchronous hota hai.", example: "emitter.emit('user:login', { userId: 1 });" },
      ], codeExamples: [
        { title: "Events Example", language: "javascript", code: "const EventEmitter = require('events');\n\nclass OrderSystem extends EventEmitter {\n    placeOrder(order) {\n        console.log('Order placed:', order.id);\n        this.emit('order:placed', order);\n        \n        setTimeout(() => {\n            this.emit('order:prepared', order);\n        }, 2000);\n        \n        setTimeout(() => {\n            this.emit('order:delivered', order);\n        }, 5000);\n    }\n}\n\nconst orders = new OrderSystem();\n\norders.on('order:placed', (order) => {\n    console.log(`Order ${order.id} confirmed`);\n});\n\norders.on('order:prepared', (order) => {\n    console.log(`Order ${order.id} is being prepared`);\n});\n\norders.once('order:delivered', (order) => {\n    console.log(`Order ${order.id} delivered! Enjoy!`);\n});\n\norders.placeOrder({ id: 101, items: ['Pizza', 'Coke'] });", output: "EventEmitter-based order system: placed -> prepared -> delivered events." },
      ], notes: ["events module core module hai", "on() se listener add, emit() se trigger", "once() ek baar ke liye listener", "Event names strings hote hain", "Error events specially handle hone chahiye"],
      commonMistakes: [{ mistake: "Error event ke bina listener ke emit karna - throws error", correction: "Hamesha error event ka listener add karein" }],
      interviewQuestions: ["EventEmitter kya hai?", "on aur once mein kya antar hai?", "Event loop events kaise handle karta hai?"],
      practiceExercises: [{ question: "Custom EventEmitter class banaye jo user registration events emit kare.", difficulty: "medium" }],
      quiz: { id: "quiz-node-events", title: "Events Quiz", questions: [
        { id: "q1", question: "Event emit karne ke liye kaunsa method use hota hai?", options: ["send()", "trigger()", "emit()", "fire()"], correctAnswer: 2, explanation: "emit() se event trigger karte hain." },
        { id: "q2", question: "One-time listener ke liye kaunsa method use hota hai?", options: ["on()", "once()", "one()", "first()"], correctAnswer: 1, explanation: "once() ek baar ke liye listener add karta hai." },
      ]}, }, duration: "15 min" },
      { id: "node-streams", title: "Streams aur Buffers", description: "Node.js streams aur buffers", order: 5, content: { introduction: "Streams Node.js mein data flow handle karne ka tarika hai. Files, network requests, process stdin/stdout sab streams use karte hain. Buffers binary data handle karte hain.", sections: [
        { title: "Stream Types", explanation: "Readable - data read (fs.createReadStream). Writable - data write (fs.createWriteStream). Duplex - both read/write (net.Socket). Transform - modify data during read/write (zlib).", example: "fs.createReadStream('large-file.txt').pipe(process.stdout);" },
        { title: "Buffer", explanation: "Buffer class raw binary data handle karta hai. Fixed size. Similar to array of integers. alloc(), from(), toString(), concat() methods. Encoding conversion (utf-8, base64, hex).", example: "const buf = Buffer.from('Hello', 'utf-8'); console.log(buf.toString('base64'));" },
      ], codeExamples: [
        { title: "Streams Example", language: "javascript", code: "const fs = require('fs');\nconst zlib = require('zlib');\nconst path = require('path');\n\n// Readable stream\nconst readStream = fs.createReadStream('input.txt', { highWaterMark: 64 * 1024 });\n\n// Writable stream\nconst writeStream = fs.createWriteStream('output.txt');\n\n// Pipe - stream chaining\nreadStream.pipe(writeStream);\n\n// Transform stream - compress\nconst gzip = zlib.createGzip();\nreadStream.pipe(gzip).pipe(fs.createWriteStream('output.txt.gz'));\n\n// Events on streams\nreadStream.on('data', (chunk) => {\n    console.log('Received chunk:', chunk.length);\n});\n\nreadStream.on('end', () => {\n    console.log('File reading complete');\n});\n\n// Buffer operations\nconst buffer1 = Buffer.from('Hello ');\nconst buffer2 = Buffer.from('World');\nconst combined = Buffer.concat([buffer1, buffer2]);\nconsole.log(combined.toString()); // Hello World\n\n// Buffer encoding\nconst str = 'Namaste Duniya!';\nconst encoded = Buffer.from(str, 'utf-8');\nconsole.log('Base64:', encoded.toString('base64'));\nconsole.log('Hex:', encoded.toString('hex'));", output: "Streams with pipe, gzip compression aur Buffer operations." },
      ], notes: ["Streams memory efficient hain large files ke liye", "Pipe method stream chaining ke liye", "Buffer binary data handle karta hai", "Encoding conversion Buffer se possible hai"],
      commonMistakes: [{ mistake: "Large files ko readFile() se read karna (memory issue)", correction: "Large files ke liye streams use karein" }],
      interviewQuestions: ["Streams kya hain?", "Stream types kaun se hain?", "Buffer kya hai?", "Pipe method kya karta hai?"],
      practiceExercises: [{ question: "Ek program likhein jo large CSV file ko read kare, process kare aur output file mein write kare using streams.", difficulty: "hard" }],
      quiz: { id: "quiz-node-streams", title: "Streams Quiz", questions: [
        { id: "q1", question: "Readable aur writable stream combine karne ke liye kaunsa method use hota hai?", options: ["link()", "connect()", "pipe()", "join()"], correctAnswer: 2, explanation: "pipe() method streams combine karta hai." },
        { id: "q2", question: "Buffer data store karne ke liye kaunsa encoding default hota hai?", options: ["utf-8", "base64", "utf16le", "latin1"], correctAnswer: 0, explanation: "Default encoding utf-8 hai." },
      ]}, }, duration: "15 min" },
      { id: "node-npm", title: "NPM aur Package Management", description: "NPM packages manage karna seekhein", order: 6, content: { introduction: "NPM (Node Package Manager) duniya ka sabse bada package registry hai. Iske through hum lakhon packages install aur manage kar sakte hain.", sections: [
        { title: "Package.json", explanation: "package.json project metadata file hai. dependencies - production packages. devDependencies - development only. scripts - custom commands. version - semantic versioning.", example: "npm init -y se package.json create hota hai." },
        { title: "NPM Commands", explanation: "npm install <package> - install. npm install -g <package> - global. npm update - update. npm uninstall - remove. npm audit - security check. npm run <script> - run scripts.", example: "npm install express mongoose dotenv" },
      ], codeExamples: [
        { title: "Package.json Example", language: "json", code: "{\n    \"name\": \"my-app\",\n    \"version\": \"1.0.0\",\n    \"description\": \"My Node.js application\",\n    \"main\": \"index.js\",\n    \"scripts\": {\n        \"start\": \"node index.js\",\n        \"dev\": \"nodemon index.js\",\n        \"test\": \"jest\",\n        \"build\": \"tsc\"\n    },\n    \"dependencies\": {\n        \"express\": \"^4.18.0\",\n        \"mongoose\": \"^7.0.0\"\n    },\n    \"devDependencies\": {\n        \"nodemon\": \"^3.0.0\",\n        \"jest\": \"^29.0.0\",\n        \"typescript\": \"^5.0.0\"\n    }\n}", output: "Standard package.json with scripts aur dependencies." },
      ], notes: ["npm init se project initialize karein", "dependencies production ke liye", "devDependencies development ke liye", "^ caret minor version updates allow karta hai", "~ tilde patch version updates allow karta hai"],
      commonMistakes: [{ mistake: "package-lock.json ko gitignore karna", correction: "package-lock.json ko commit karna chahiye for reproducible builds" }],
      interviewQuestions: ["dependencies aur devDependencies mein kya antar hai?", "Semver (semantic versioning) kya hai?", "package-lock.json kya hai?"],
      practiceExercises: [{ question: "Node.js project initialize karein, kuch packages install karein, aur npm scripts configure karein.", difficulty: "easy" }],
      quiz: { id: "quiz-node-npm", title: "NPM Quiz", questions: [
        { id: "q1", question: "Production dependency install karne ke liye kaunsa flag use hota hai?", options: ["--save", "--dev", "--prod", "--global"], correctAnswer: 2, explanation: "Default install --prod hota hai." },
        { id: "q2", question: "Security issues check karne ke liye kaunsa command use hota hai?", options: ["npm check", "npm audit", "npm security", "npm verify"], correctAnswer: 1, explanation: "npm audit security check karta hai." },
      ]}, }, duration: "15 min" },
      { id: "node-process", title: "Process aur Environment", description: "Node.js process object aur env variables", order: 7, content: { introduction: "process object Node.js application ke baare mein information provide karta hai. Environment variables, command line arguments, process lifecycle handle karte hain.", sections: [
        { title: "Process Object", explanation: "process.env - environment variables. process.argv - CLI arguments. process.cwd() - current directory. process.exit() - exit. process.on() - event listeners (exit, uncaughtException). process.nextTick() - next microtask.", example: "console.log(process.env.NODE_ENV); console.log(process.argv);" },
        { title: "Environment Variables", explanation: "Sensitive data (API keys, passwords, DB URLs) environment variables mein store karte hain. dotenv package .env file se load karta hai. process.env se access karte hain.", example: "DB_URL=localhost:27017 node app.js" },
      ], codeExamples: [
        { title: "Process and Environment", language: "javascript", code: "// .env file mein:\n// PORT=3000\n// DB_URL=mongodb://localhost:27017/myapp\n// API_KEY=secret123\n\nrequire('dotenv').config();\n\nconst PORT = process.env.PORT || 3000;\nconst DB_URL = process.env.DB_URL;\n\nconsole.log('Running on port:', PORT);\nconsole.log('Database:', DB_URL);\n\n// Command line arguments\n// node app.js --name=Rahul --age=25\nconst args = process.argv.slice(2);\nconsole.log('Arguments:', args);\n\n// Process events\nprocess.on('uncaughtException', (err) => {\n    console.error('Uncaught error:', err);\n    process.exit(1);\n});\n\nprocess.on('exit', (code) => {\n    console.log('Exiting with code:', code);\n});\n\n// Current directory\nconsole.log('Current dir:', process.cwd());\n\n// Memory usage\nconsole.log('Memory:', process.memoryUsage());", output: "Environment variables, CLI args, process events aur system info." },
      ], notes: ["process object global hai", "Environment variables .env file mein store karein", "dotenv package install karein for .env", "uncaughtException handler process crash se bachata hai"],
      commonMistakes: [{ mistake: "Sensitive data ko code mein hardcode karna", correction: "Hamesha environment variables use karein" }],
      interviewQuestions: ["process.env kya hai?", "Environment variables kyun use karte hain?", "dotenv package kya karta hai?"],
      practiceExercises: [{ question: "Environment variables ka use karke configuration manage karein (.env file with dotenv).", difficulty: "easy" }],
      quiz: { id: "quiz-node-process", title: "Process Quiz", questions: [
        { id: "q1", question: "Current directory pata karne ke liye kaunsa method use hota hai?", options: ["process.dir()", "process.cwd()", "process.pwd()", "process.path()"], correctAnswer: 1, explanation: "process.cwd() current directory batata hai." },
        { id: "q2", question: "Process exit karne ke liye kaunsa method use hota hai?", options: ["process.kill()", "process.exit()", "process.stop()", "process.quit()"], correctAnswer: 1, explanation: "process.exit() se process terminate hota hai." },
      ]}, }, duration: "15 min" },
      { id: "node-http", title: "HTTP Module aur Web Servers", description: "Node.js HTTP module se servers", order: 8, content: { introduction: "http module Node.js ke core modules mein se hai. Isse HTTP servers create kar sakte hain jo requests handle karte hain aur responses bhejte hain.", sections: [
        { title: "Creating HTTP Server", explanation: "http.createServer() se server banate hain. Request listener function har request par call hota hai. req object request details, res object response ke liye. res.end() se response bhejte hain.", example: "http.createServer((req, res) => { res.end('OK'); }).listen(3000);" },
        { title: "Request aur Response", explanation: "req.url - requested URL. req.method - HTTP method (GET, POST). req.headers - request headers. res.writeHead() - status + headers. res.write() - response body. res.end() - end response.", example: "res.writeHead(200, { 'Content-Type': 'application/json' });" },
      ], codeExamples: [
        { title: "HTTP Server", language: "javascript", code: "const http = require('http');\nconst url = require('url');\n\nconst server = http.createServer((req, res) => {\n    const parsedUrl = url.parse(req.url, true);\n    const path = parsedUrl.pathname;\n    const method = req.method;\n\n    console.log(`${method} ${path}`);\n\n    // Basic routing\n    if (path === '/' && method === 'GET') {\n        res.writeHead(200, { 'Content-Type': 'text/html' });\n        res.end('<h1>Home Page</h1>');\n    } else if (path === '/api/users' && method === 'GET') {\n        const users = [\n            { id: 1, name: 'Rahul' },\n            { id: 2, name: 'Priya' }\n        ];\n        res.writeHead(200, { 'Content-Type': 'application/json' });\n        res.end(JSON.stringify(users));\n    } else if (path === '/api/users' && method === 'POST') {\n        let body = '';\n        req.on('data', chunk => body += chunk);\n        req.on('end', () => {\n            const data = JSON.parse(body);\n            res.writeHead(201, { 'Content-Type': 'application/json' });\n            res.end(JSON.stringify({ message: 'User created', user: data }));\n        });\n    } else {\n        res.writeHead(404);\n        res.end('Not Found');\n    }\n});\n\nserver.listen(3000, () => {\n    console.log('Server running at http://localhost:3000');\n});", output: "HTTP server with routing for different paths aur methods." },
      ], notes: ["http.createServer() se server banate hain", "req.url aur req.method se routing karein", "res.end() se response bhejte hain", "Complex routing ke liye Express use karein"],
      commonMistakes: [{ mistake: "res.end() ke baad res.write() call karna", correction: "res.end() ke baad response complete hota hai, pehle res.write() karein" }],
      interviewQuestions: ["HTTP server kaise banate hain?", "req.url aur req.method kya hai?", "res.writeHead kya karta hai?"],
      practiceExercises: [{ question: "HTTP server banaye with CRUD API routes for users (GET, POST, PUT, DELETE).", difficulty: "medium" }],
      quiz: { id: "quiz-node-http", title: "HTTP Module Quiz", questions: [
        { id: "q1", question: "HTTP server banane ke liye kaunsa method use hota hai?", options: ["http.createServer()", "http.newServer()", "http.Server()", "http.start()"], correctAnswer: 0, explanation: "http.createServer() se server banata hai." },
        { id: "q2", question: "Response headers set karne ke liye kaunsa method use hota hai?", options: ["res.headers()", "res.writeHead()", "res.setHeader()", "res.send()"], correctAnswer: 1, explanation: "res.writeHead() status aur headers set karta hai." },
      ]}, }, duration: "20 min" },
      { id: "node-express-intro", title: "Express.js Integration", description: "Node.js aur Express ka integration", order: 9, content: { introduction: "Express.js Node.js ka most popular web framework hai. Ye HTTP module se zyada clean aur powerful API provide karta hai. Routes, middleware, static files sab easy banata hai.", sections: [
        { title: "Express Setup", explanation: "npm install express. app = express(). app.get('/path', handler). app.listen(port). next section mein Express ka detailed course hai.", example: "const express = require('express'); const app = express();" },
      ], codeExamples: [
        { title: "Basic Express Server", language: "javascript", code: "const express = require('express');\nconst app = express();\nconst PORT = process.env.PORT || 3000;\n\napp.use(express.json()); // JSON body parser\n\napp.get('/', (req, res) => {\n    res.json({ message: 'Welcome to Express!' });\n});\n\napp.get('/api/users', (req, res) => {\n    res.json([{ id: 1, name: 'Rahul' }]);\n});\n\napp.listen(PORT, () => {\n    console.log(`Server running on port ${PORT}`);\n});", output: "Basic Express server setup." },
      ], notes: ["Express Node.js ka popular framework hai", "npm install express se install karein", "app.get/post/put/delete se routes", "express.json() body parser middleware"],
      commonMistakes: [{ mistake: "Express aur HTTP module ko mix karna", correction: "Express use karte waqt http.createServer ki zaroorat nahi" }],
      interviewQuestions: ["Express kya hai?", "Node.js HTTP module se Express kaise better hai?"],
      practiceExercises: [{ question: "Express server setup karein with basic routes - GET, POST, PUT, DELETE.", difficulty: "easy" }],
      quiz: { id: "quiz-node-express", title: "Express Integration Quiz", questions: [
        { id: "q1", question: "JSON body parse karne ke liye Express mein kaunsa middleware use hota hai?", options: ["bodyParser.json()", "express.json()", "express.body()", "json.parse()"], correctAnswer: 1, explanation: "express.json() middleware JSON body parse karta hai." },
      ]}, }, duration: "10 min" },
      { id: "node-debugging", title: "Debugging aur Error Handling", description: "Node.js debugging aur error handling", order: 10, content: { introduction: "Debugging aur error handling production-grade Node.js applications ke liye essential hai. Proper error handling se application crash aur data loss se bachti hai.", sections: [
        { title: "Error Handling Patterns", explanation: "Try-catch async functions mein. .catch() promises ke liye. Express error middleware. uncaughtException aur unhandledRejection global handlers. Process exit after uncaughtException for clean state.", example: "app.use((err, req, res, next) => { console.error(err); res.status(500).json({ error: err.message }); });" },
        { title: "Debugging Tools", explanation: "console.log() - simple. Node.js inspector (--inspect flag). Chrome DevTools. VS Code debugger. Nodemon for auto-restart. Logging libraries (winston, pino).", example: "node --inspect-brk index.js (Chrome DevTools connect)" },
      ], codeExamples: [
        { title: "Error Handling", language: "javascript", code: "// Async error wrapper\nfunction asyncHandler(fn) {\n    return (req, res, next) => {\n        Promise.resolve(fn(req, res, next)).catch(next);\n    };\n}\n\n// Custom error class\nclass AppError extends Error {\n    constructor(message, statusCode) {\n        super(message);\n        this.statusCode = statusCode;\n        this.isOperational = true;\n    }\n}\n\n// Express error middleware\napp.use((err, req, res, next) => {\n    err.statusCode = err.statusCode || 500;\n    err.message = err.message || 'Internal Server Error';\n    \n    if (err.isOperational) {\n        res.status(err.statusCode).json({\n            status: 'error',\n            message: err.message\n        });\n    } else {\n        // Programming or unknown error - log aur generic response\n        console.error('ERROR:', err);\n        res.status(500).json({\n            status: 'error',\n            message: 'Something went wrong!'\n        });\n    }\n});\n\n// Global handlers\nprocess.on('uncaughtException', (err) => {\n    console.error('Uncaught Exception:', err);\n    process.exit(1);\n});\n\nprocess.on('unhandledRejection', (reason) => {\n    console.error('Unhandled Rejection:', reason);\n    process.exit(1);\n});", output: "Production-ready error handling patterns." },
      ], notes: ["Async errors properly handle karein", "Global error handlers for uncaught exceptions", "Use error classes for different error types", "Logging for debugging aur monitoring"],
      commonMistakes: [{ mistake: "uncaughtException mein process na exit karna (unstable state)", correction: "uncaughtException par process exit karein, cleanup karein" }],
      interviewQuestions: ["Error handling patterns kya hain?", "uncaughtException aur unhandledRejection mein kya antar hai?"],
      practiceExercises: [{ question: "Express app mein global error handling middleware implement karein with custom error classes.", difficulty: "hard" }],
      quiz: { id: "quiz-node-debug", title: "Debugging Quiz", questions: [
        { id: "q1", question: "Node.js debugging ke liye kaunsa flag use hota hai?", options: ["--debug", "--inspect", "--dev", "--verbose"], correctAnswer: 1, explanation: "--inspect flag debugging ke liye." },
      ]}, }, duration: "20 min" },
    ],
  }],
}
