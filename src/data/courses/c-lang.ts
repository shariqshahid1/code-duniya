import { Course } from "@/types"

export const cCourse: Course = {
  id: "c-lang", title: "C Programming",
  description: "C language seekhein - Programming ki buniyad mazboot karein",
  longDescription: "C ek powerful programming language hai jo system programming, embedded systems, aur operating systems mein use hoti hai. Is course mein aap C basics, pointers, memory management, data structures seekhenge.",
  category: "programming-languages", icon: "Terminal", color: "from-gray-600 to-blue-600",
  difficulty: "intermediate", lessonsCount: 10, duration: "5 hours",
  prerequisites: ["Basic programming logic"],
  modules: [{
    id: "c-core", title: "C Programming Basics", description: "C fundamentals", order: 1, lessons: [
      { id: "c-what-is", title: "C Language Kya Hai?", description: "C introduction", order: 1, content: { introduction: "C ek procedural programming language hai jo Dennis Ritchie ne 1972 mein AT&T Bell Labs mein banayi. Ye system programming ki base language hai.", sections: [
        { title: "C Overview", explanation: "C middle-level language hai (high-level + low-level features). Compiled language (executable file banta hai). Structured programming language. Operating systems, embedded systems, compilers C mein likhe hain. Fast aur efficient.", example: "Unix OS C mein likha gaya hai." },
      ], codeExamples: [
        { title: "Pehla C Program", language: "c", code: "#include <stdio.h>\n\nint main() {\n    printf(\"Namaste Duniya!\\n\");\n    return 0;\n}", output: "Ye program 'Namaste Duniya!' print karega." },
      ], notes: ["C compiled language hai", "stdio.h standard input-output header hai", "main() program entry point hai", "return 0 means successful execution"],
      commonMistakes: [{ mistake: "Semicolon bhoolna (;)", correction: "Har statement ke end mein semicolon dena" }],
      interviewQuestions: ["C kya hai?", "Compiled aur interpreted language mein kya antar hai?", "main() function kya hai?"],
      practiceExercises: [{ question: "C program likhein jo aapka naam aur class print kare.", difficulty: "easy" }],
      quiz: { id: "quiz-c-intro", title: "C Intro Quiz", questions: [
        { id: "q1", question: "C programming language ko kisne banaya?", options: ["Dennis Ritchie", "Bjarne Stroustrup", "Ken Thompson", "James Gosling"], correctAnswer: 0, explanation: "Dennis Ritchie ne C banaya." },
      ]}, }, duration: "15 min" },
      { id: "c-variables", title: "Variables aur Data Types", description: "C data types aur variables", order: 2, content: { introduction: "C mein statically typed variables hote hain - aapko type declare karna padta hai. Basic types: int, float, double, char.", sections: [
        { title: "Data Types", explanation: "int (2/4 bytes), float (4 bytes), double (8 bytes), char (1 byte). signed/unsigned. short/long. sizeof() operator size batata hai. Format specifiers: %d (int), %f (float), %lf (double), %c (char), %s (string).", example: "int age = 25; float pi = 3.14; char grade = 'A';" },
        { title: "Constants aur Type Modifiers", explanation: "const keyword for constant value. #define preprocessor constant. Type modifiers: short, long, signed, unsigned. Enum for named constants.", example: "const float PI = 3.14159; #define MAX 100" },
      ], codeExamples: [
        { title: "Variables Example", language: "c", code: "#include <stdio.h>\n\nint main() {\n    // Variable declaration and initialization\n    int age = 25;\n    float height = 5.9;\n    double pi = 3.1415926535;\n    char grade = 'A';\n    char name[] = \"Rahul\";\n    \n    // Print values\n    printf(\"Name: %s\\n\", name);\n    printf(\"Age: %d\\n\", age);\n    printf(\"Height: %.1f\\n\", height);\n    printf(\"Pi: %.10lf\\n\", pi);\n    printf(\"Grade: %c\\n\", grade);\n    \n    // Constants\n    const int DAYS_IN_WEEK = 7;\n    printf(\"Days in week: %d\\n\", DAYS_IN_WEEK);\n    \n    // sizeof\n    printf(\"Size of int: %zu bytes\\n\", sizeof(int));\n    printf(\"Size of double: %zu bytes\\n\", sizeof(double));\n    \n    return 0;\n}", output: "Variables declaration, initialization aur printing." },
      ], notes: ["C statically typed hai", "%d, %f, %c format specifiers", "const keyword for constants", "#define preprocessor directive"],
      commonMistakes: [{ mistake: "Format specifier galat dena (int ke liye %f)", correction: "Sahi format specifier use karein" }],
      interviewQuestions: ["Data types kaun se hain?", "signed aur unsigned mein kya antar hai?", "sizeof() kya karta hai?"],
      practiceExercises: [{ question: "Program likhein jo sabhi basic data types ke variables declare kare aur unke sizes print kare.", difficulty: "easy" }],
      quiz: { id: "quiz-c-vars", title: "Variables Quiz", questions: [
        { id: "q1", question: "Float variable print karne ke liye format specifier kaunsa hai?", options: ["%d", "%f", "%c", "%s"], correctAnswer: 1, explanation: "%f float ke liye format specifier hai." },
      ]}, }, duration: "15 min" },
      { id: "c-control", title: "Control Flow", description: "C conditionals and loops", order: 3, content: { introduction: "Control flow statements program mein decisions lene aur repetitive tasks karne ke liye use hote hain.", sections: [
        { title: "If-Else aur Switch", explanation: "if, else if, else. Switch-case: integer/char expression ke basis par jump. switch faster than multiple if-else for discrete values. break statement fall-through rokta hai.", example: "if (age >= 18) { printf('Adult'); } else { printf('Minor'); }" },
        { title: "Loops", explanation: "for (init; condition; increment) { }. while (condition) { }. do { } while (condition). break - exit loop. continue - skip iteration. Nested loops.", example: "for (int i = 0; i < 5; i++) { printf('%d ', i); }" },
      ], codeExamples: [
        { title: "Control Flow", language: "c", code: "#include <stdio.h>\n\nint main() {\n    int score = 85;\n    \n    // If-Else\n    if (score >= 90) {\n        printf(\"Grade A\\n\");\n    } else if (score >= 80) {\n        printf(\"Grade B\\n\");\n    } else {\n        printf(\"Grade C\\n\");\n    }\n    \n    // Switch\n    int day = 3;\n    switch (day) {\n        case 1: printf(\"Monday\\n\"); break;\n        case 2: printf(\"Tuesday\\n\"); break;\n        case 3: printf(\"Wednesday\\n\"); break;\n        default: printf(\"Other day\\n\");\n    }\n    \n    // For loop\n    printf(\"Numbers: \");\n    for (int i = 1; i <= 5; i++) {\n        printf(\"%d \", i);\n    }\n    printf(\"\\n\");\n    \n    // While loop\n    int i = 0;\n    while (i < 5) {\n        printf(\"%d \", i);\n        i++;\n    }\n    printf(\"\\n\");\n    \n    // Do-While (executes at least once)\n    int j = 0;\n    do {\n        printf(\"%d \", j);\n        j++;\n    } while (j < 5);\n    printf(\"\\n\");\n    \n    // Break and Continue\n    for (int k = 0; k < 10; k++) {\n        if (k == 3) continue;\n        if (k == 7) break;\n        printf(\"%d \", k);\n    }\n    printf(\"\\n\");\n    \n    return 0;\n}", output: "Control flow examples with all loop types." },
      ], notes: ["if-else aur switch decision making", "for loop jab iterations pata hon", "while loop condition-based", "break aur continue loop control"],
      commonMistakes: [{ mistake: "Switch mein break bhoolna (fall-through)", correction: "Har case mein break dena" }],
      interviewQuestions: ["For, while, do-while mein kya antar hai?", "Break aur continue mein kya difference hai?"],
      practiceExercises: [{ question: "FizzBuzz program likhein C mein - 1 se 100 tak, 3 se divisible Fizz, 5 se Buzz.", difficulty: "easy" }],
      quiz: { id: "quiz-c-control", title: "Control Flow Quiz", questions: [
        { id: "q1", question: "Switch statement mein fall-through rokne ke liye kaunsa keyword use hota hai?", options: ["stop", "break", "exit", "continue"], correctAnswer: 1, explanation: "break fall-through rokta hai." },
      ]}, }, duration: "15 min" },
      { id: "c-arrays", title: "Arrays aur Strings", description: "C arrays and strings", order: 4, content: { introduction: "Arrays same type ke multiple elements ka collection hote hain. Strings C mein character arrays hote hain.", sections: [
        { title: "Arrays", explanation: "type name[size]. 0-indexed. Declaration: int arr[5];. Initialization: int arr[] = {1,2,3};. Multi-dimensional: int matrix[3][3];. No bounds checking - programmer responsibility.", example: "int numbers[5] = {10, 20, 30, 40, 50}; numbers[0] = 100;" },
        { title: "Strings", explanation: "char str[] = 'Hello';. Null-terminated ('\\0'). string.h functions: strlen(), strcpy(), strcat(), strcmp(). gets()/puts() for I/O (unsafe). fgets() safe input.", example: "char name[50]; printf('Enter name: '); fgets(name, 50, stdin);" },
      ], codeExamples: [
        { title: "Arrays and Strings", language: "c", code: "#include <stdio.h>\n#include <string.h>\n\nint main() {\n    // Arrays\n    int numbers[5] = {10, 20, 30, 40, 50};\n    printf(\"First element: %d\\n\", numbers[0]);\n    \n    // Array iteration\n    for (int i = 0; i < 5; i++) {\n        printf(\"%d \", numbers[i]);\n    }\n    printf(\"\\n\");\n    \n    // Multi-dimensional array\n    int matrix[2][3] = {{1,2,3}, {4,5,6}};\n    for (int i = 0; i < 2; i++) {\n        for (int j = 0; j < 3; j++) {\n            printf(\"%d \", matrix[i][j]);\n        }\n        printf(\"\\n\");\n    }\n    \n    // Strings\n    char str1[] = \"Hello\";\n    char str2[50];\n    \n    printf(\"String: %s\\n\", str1);\n    printf(\"Length: %lu\\n\", strlen(str1));\n    \n    // String operations\n    strcpy(str2, str1);\n    strcat(str2, \" World\");\n    printf(\"Copied and concatenated: %s\\n\", str2);\n    \n    // String comparison\n    if (strcmp(str1, str2) == 0) {\n        printf(\"Strings equal\\n\");\n    } else {\n        printf(\"Strings different\\n\");\n    }\n    \n    // Safe input\n    char name[50];\n    printf(\"Enter name: \");\n    fgets(name, 50, stdin);\n    name[strcspn(name, \"\\n\")] = 0; // remove newline\n    printf(\"Hello %s!\\n\", name);\n    \n    return 0;\n}", output: "Arrays, multi-dimensional arrays, strings, string.h functions." },
      ], notes: ["Arrays 0-indexed hote hain", "String null-terminated character array hai", "string.h mein string functions hain", "Array bounds check nahi hota (segfault possible)"],
      commonMistakes: [{ mistake: "Array size se bahar access karna (buffer overflow)", correction: "Hamesha array bounds check karein" }],
      interviewQuestions: ["Array kya hai?", "String C mein kaise implement hota hai?", "strcpy() aur strncpy() mein kya antar hai?"],
      practiceExercises: [{ question: "Program likhein jo array ke elements ko reverse kare. String palindrome check karein.", difficulty: "medium" }],
      quiz: { id: "quiz-c-arrays", title: "Arrays Quiz", questions: [
        { id: "q1", question: "String length nikalne ke liye kaunsa function use hota hai?", options: ["strlength()", "strlen()", "length()", "sizeof()"], correctAnswer: 1, explanation: "strlen() string length batata hai." },
      ]}, }, duration: "15 min" },
      { id: "c-pointers", title: "Pointers", description: "C pointers aur memory management", order: 5, content: { introduction: "Pointers C ka powerful feature hai jo memory addresses store karta hai. Ye direct memory manipulation allow karta hai.", sections: [
        { title: "Pointer Basics", explanation: "& address-of operator. * dereference operator. type* ptr = &var;. Pointer size 4/8 bytes (32/64-bit). NULL pointer (points to nothing). Pointer ke types: int*, char*, void*. void* generic pointer.", example: "int x = 10; int* ptr = &x; printf(\"%d\", *ptr);" },
        { title: "Pointer Arithmetic", explanation: "ptr++ - next element (size of type). ptr + n - n elements ahead. ptr2 - ptr1 - number of elements between. Array name is pointer to first element. arr[i] = *(arr + i).", example: "int arr[5] = {1,2,3,4,5}; int* p = arr; p++; printf(\"%d\", *p);" },
      ], codeExamples: [
        { title: "Pointers Example", language: "c", code: "#include <stdio.h>\n\nint main() {\n    int x = 10;\n    int* ptr = &x;\n    \n    printf(\"x = %d\\n\", x);\n    printf(\"Address of x: %p\\n\", (void*)&x);\n    printf(\"ptr = %p\\n\", (void*)ptr);\n    printf(\"*ptr = %d\\n\", *ptr);\n    \n    // Modify via pointer\n    *ptr = 20;\n    printf(\"After *ptr = 20, x = %d\\n\", x);\n    \n    // Pointer arithmetic\n    int arr[] = {10, 20, 30, 40, 50};\n    int* p = arr;\n    \n    for (int i = 0; i < 5; i++) {\n        printf(\"arr[%d] = %d (via pointer: %d)\\n\", i, arr[i], *(p + i));\n    }\n    \n    // Pointer to pointer\n    int y = 42;\n    int* p1 = &y;\n    int** p2 = &p1;\n    printf(\"**p2 = %d\\n\", **p2);\n    \n    // Function with pointer (modify original)\n    void increment(int* n) {\n        (*n)++;\n    }\n    \n    int num = 5;\n    increment(&num);\n    printf(\"After increment: %d\\n\", num);\n    \n    // Array as function parameter\n    void printArray(int* arr, int size) {\n        for (int i = 0; i < size; i++) {\n            printf(\"%d \", *arr++);\n        }\n        printf(\"\\n\");\n    }\n    \n    printArray(arr, 5);\n    \n    return 0;\n}", output: "Pointers, pointer arithmetic, double pointers, pointers with functions." },
      ], notes: ["& -> address of, * -> value at address", "Pointer arithmetic type size depend karta hai", "Array name decays to pointer", "NULL pointer initialization important hai"],
      commonMistakes: [{ mistake: "Dereference NULL pointer (segmentation fault)", correction: "Hamesha NULL check karein dereference se pehle" }],
      interviewQuestions: ["Pointer kya hai?", "Pointer arithmetic kaise kaam karta hai?", "Array aur pointer relation kya hai?"],
      practiceExercises: [{ question: "Function likhein jo array aur pointer dono se sum calculate kare. Swap function pointer se implement karein.", difficulty: "hard" }],
      quiz: { id: "quiz-c-pointers", title: "Pointers Quiz", questions: [
        { id: "q1", question: "Address-of operator kaunsa hai?", options: ["*", "&", "%", "#"], correctAnswer: 1, explanation: "& address-of operator hai." },
      ]}, }, duration: "20 min" },
      { id: "c-functions", title: "Functions aur Scope", description: "C functions aur variable scope", order: 6, content: { introduction: "Functions C mein code reusability ke liye hain. Har function ek specific task karta hai. main() se program start hota hai.", sections: [
        { title: "Function Structure", explanation: "return_type function_name(parameters) { body }. Function prototype (declaration) before use. void return for no value. Call by value (default). Call by reference (using pointers).", example: "int add(int a, int b); // prototype; int result = add(5, 3);" },
        { title: "Scope aur Storage Classes", explanation: "Local scope - block ke andar. Global scope - file level. auto - default (local). register - suggest CPU register. static - lifetime whole program. extern - variable defined elsewhere.", example: "int global = 10; // global variable. static int counter = 0; // static variable" },
      ], codeExamples: [
        { title: "Functions Example", language: "c", code: "#include <stdio.h>\n\n// Function prototype\nint factorial(int n);\nvoid swap(int* a, int* b);\nint global_count = 0;\n\n// Function definition\nint add(int a, int b) {\n    global_count++;\n    return a + b;\n}\n\n// Recursive function\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\n// Call by reference\nvoid swap(int* a, int* b) {\n    int temp = *a;\n    *a = *b;\n    *b = temp;\n}\n\n// Static variable\nvoid counter() {\n    static int count = 0;\n    count++;\n    printf(\"Called %d times\\n\", count);\n}\n\nint main() {\n    printf(\"Add: %d\\n\", add(10, 20));\n    printf(\"Factorial of 5: %d\\n\", factorial(5));\n    \n    int x = 10, y = 20;\n    printf(\"Before swap: x=%d, y=%d\\n\", x, y);\n    swap(&x, &y);\n    printf(\"After swap: x=%d, y=%d\\n\", x, y);\n    \n    counter();\n    counter();\n    counter();\n    \n    printf(\"Global count: %d\\n\", global_count);\n    \n    return 0;\n}", output: "Functions, recursion, call by reference, static variables." },
      ], notes: ["Functions reusable code blocks hain", "Function prototype declaration ke liye", "Call by value default, call by reference pointers se", "static variable lifetime whole program"],
      commonMistakes: [{ mistake: "Function prototype bhoolna (compiler warning/error)", correction: "Function ko use karne se pehle declare/prototype karein" }],
      interviewQuestions: ["Call by value aur call by reference mein kya antar hai?", "static variable kya hai?", "Recursion kya hai?"],
      practiceExercises: [{ question: "Recursive function se Fibonacci series calculate karein. aur swap function implement karein.", difficulty: "medium" }],
      quiz: { id: "quiz-c-functions", title: "Functions Quiz", questions: [
        { id: "q1", question: "Call by reference implement karne ke liye kaunsa concept use hota hai?", options: ["Arrays", "Pointers", "Structs", "Enums"], correctAnswer: 1, explanation: "Pointers se call by reference implement karte hain." },
      ]}, }, duration: "15 min" },
      { id: "c-structs", title: "Structures aur Unions", description: "C structures aur unions", order: 7, content: { introduction: "Structures different types ke variables ka collection hote hain. Unions memory share karte hain. Ye user-defined data types hain.", sections: [
        { title: "Structures", explanation: "struct keyword se define. struct Student { int id; char name[50]; };. Member access via dot (.) operator. Arrow (->) operator for pointer to struct. typedef for alias. Nested structures.", example: "struct Student s1 = {1, 'Rahul'}; s1.id = 2;" },
        { title: "Unions aur enum", explanation: "union similar to struct but memory share karta hai (largest member size). Ek time par ek hi member use kar sakte hain. enum integer constants ke named set ke liye.", example: "union Data { int i; float f; }; enum Color { RED, GREEN, BLUC };" },
      ], codeExamples: [
        { title: "Structures Example", language: "c", code: "#include <stdio.h>\n#include <string.h>\n\n// Structure definition\nstruct Student {\n    int id;\n    char name[50];\n    float marks;\n};\n\n// Typedef for alias\ntypedef struct {\n    int x;\n    int y;\n} Point;\n\n// Nested structure\nstruct Address {\n    char city[50];\n    char state[50];\n};\n\nstruct Employee {\n    int id;\n    char name[50];\n    struct Address addr;\n};\n\nint main() {\n    // Structure initialization\n    struct Student s1 = {1, \"Rahul\", 85.5};\n    struct Student s2;\n    \n    // Access members\n    printf(\"Student ID: %d\\n\", s1.id);\n    printf(\"Name: %s\\n\", s1.name);\n    printf(\"Marks: %.1f\\n\", s1.marks);\n    \n    // Modify members\n    s2.id = 2;\n    strcpy(s2.name, \"Priya\");\n    s2.marks = 92.0;\n    \n    // Pointer to structure (-> operator)\n    struct Student* ptr = &s1;\n    printf(\"Via pointer: %s\\n\", ptr->name);\n    \n    // Array of structures\n    struct Student batch[3] = {\n        {1, \"Rahul\", 85},\n        {2, \"Priya\", 92},\n        {3, \"Amit\", 78}\n    };\n    \n    for (int i = 0; i < 3; i++) {\n        printf(\"%d: %s - %.1f%%\\n\", batch[i].id, batch[i].name, batch[i].marks);\n    }\n    \n    // Typedef\n    Point p1 = {10, 20};\n    printf(\"Point: (%d, %d)\\n\", p1.x, p1.y);\n    \n    // Nested structure\n    struct Employee emp = {101, \"Rahul\", {\"Delhi\", \"Delhi\"}};\n    printf(\"%s from %s, %s\\n\", emp.name, emp.addr.city, emp.addr.state);\n    \n    // Union\n    union Data {\n        int i;\n        float f;\n        char str[20];\n    };\n    \n    union Data data;\n    data.i = 10;\n    printf(\"Data as int: %d\\n\", data.i);\n    data.f = 3.14;\n    printf(\"Data as float: %.2f (int corrupted: %d)\\n\", data.f, data.i);\n    \n    return 0;\n}", output: "Structures, nested structures, arrays of structures, unions." },
      ], notes: ["struct different data types ka collection hai", "union memory share karta hai", "typedef se alias banate hain", "Structure members dot (.) se access karte hain"],
      commonMistakes: [{ mistake: "Structure ko pointer se access karte waqt dot (.) use karna", correction: "Arrow (->) operator use karein ya (*ptr).member" }],
      interviewQuestions: ["Structure aur union mein kya antar hai?", "typedef kya hai?", "Structure padding kya hai?"],
      practiceExercises: [{ question: "Employee management system ke liye structures banayein - nested structures, array of structures ke saath.", difficulty: "medium" }],
      quiz: { id: "quiz-c-structs", title: "Structures Quiz", questions: [
        { id: "q1", question: "Pointer se structure member access ke liye kaunsa operator use hota hai?", options: [".", "->", "::", "=>"], correctAnswer: 1, explanation: "-> arrow operator pointer se member access karta hai." },
      ]}, }, duration: "20 min" },
      { id: "c-file", title: "File Handling", description: "C file I/O operations", order: 8, content: { introduction: "C mein file handling stdio.h library ke functions se karte hain. FILE* pointer file operations ke liye use hota hai.", sections: [
        { title: "File Operations", explanation: "fopen() - file open. Modes: 'r' (read), 'w' (write), 'a' (append), 'rb'/'wb' (binary). fclose() - file close. fprintf() - formatted write. fscanf() - formatted read. fgets() - read line. fputs() - write line.", example: "FILE* f = fopen('data.txt', 'w'); fprintf(f, 'Hello'); fclose(f);" },
      ], codeExamples: [
        { title: "File Handling", language: "c", code: "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    FILE* f;\n    char line[100];\n    \n    // Writing to file\n    f = fopen(\"data.txt\", \"w\");\n    if (f == NULL) {\n        printf(\"File nahi khul sakta!\\n\");\n        return 1;\n    }\n    fprintf(f, \"Line 1: Hello World\\n\");\n    fprintf(f, \"Line 2: Namaste Duniya\\n\");\n    fputs(\"Line 3: Direct write\\n\", f);\n    fclose(f);\n    printf(\"File written successfully\\n\");\n    \n    // Reading from file\n    f = fopen(\"data.txt\", \"r\");\n    if (f == NULL) {\n        printf(\"File nahi mila!\\n\");\n        return 1;\n    }\n    \n    printf(\"\\nFile contents:\\n\");\n    while (fgets(line, sizeof(line), f)) {\n        printf(\"%s\", line);\n    }\n    fclose(f);\n    \n    // Append mode\n    f = fopen(\"data.txt\", \"a\");\n    fprintf(f, \"Line 4: Appended text\\n\");\n    fclose(f);\n    \n    // Binary file write\n    typedef struct {\n        int id;\n        char name[50];\n    } Record;\n    \n    Record records[] = {{1, \"Rahul\"}, {2, \"Priya\"}};\n    \n    f = fopen(\"records.bin\", \"wb\");\n    fwrite(records, sizeof(Record), 2, f);\n    fclose(f);\n    \n    // Binary file read\n    Record read_records[2];\n    f = fopen(\"records.bin\", \"rb\");\n    fread(read_records, sizeof(Record), 2, f);\n    fclose(f);\n    \n    for (int i = 0; i < 2; i++) {\n        printf(\"Record %d: %d - %s\\n\", i+1, read_records[i].id, read_records[i].name);\n    }\n    \n    return 0;\n}", output: "File I/O with text and binary modes." },
      ], notes: ["fopen() file open karta hai", "Always check if file opened successfully", "fclose() file close karna zaroori hai", "Binary files for structured data"],
      commonMistakes: [{ mistake: "fclose() bhoolna (memory leak)", correction: "Hamesha fclose() call karein" }],
      interviewQuestions: ["File modes kaun se hain?", "Text aur binary file mein kya antar hai?"],
      practiceExercises: [{ question: "Student records ko file mein save aur load karein binary mode mein.", difficulty: "medium" }],
      quiz: { id: "quiz-c-files", title: "File Handling Quiz", questions: [
        { id: "q1", question: "File open karne ke liye kaunsa function use hota hai?", options: ["fopen()", "open()", "fcreate()", "file_open()"], correctAnswer: 0, explanation: "fopen() file open karta hai." },
      ]}, }, duration: "15 min" },
      { id: "c-dynamic", title: "Dynamic Memory Allocation", description: "malloc, calloc, realloc, free", order: 9, content: { introduction: "C mein dynamic memory allocation runtime par memory allocate karne ki facility deta hai. Heap memory se allocate hota hai.", sections: [
        { title: "Memory Functions", explanation: "malloc(size) - memory allocate (uninitialized). calloc(n, size) - allocate + zero-initialized. realloc(ptr, new_size) - resize. free(ptr) - deallocate. stdlib.h header. #include <stdlib.h>. Always check return value for NULL.", example: "int* arr = (int*)malloc(5 * sizeof(int)); free(arr);" },
      ], codeExamples: [
        { title: "Dynamic Memory", language: "c", code: "#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int n;\n    \n    printf(\"Array size: \");\n    scanf(\"%d\", &n);\n    \n    // malloc - allocate memory\n    int* arr = (int*)malloc(n * sizeof(int));\n    if (arr == NULL) {\n        printf(\"Memory allocation failed!\\n\");\n        return 1;\n    }\n    \n    // Initialize and display\n    for (int i = 0; i < n; i++) {\n        arr[i] = (i + 1) * 10;\n        printf(\"%d \", arr[i]);\n    }\n    printf(\"\\n\");\n    \n    // calloc - zero-initialized\n    int* arr2 = (int*)calloc(n, sizeof(int));\n    // arr2[i] = 0 automatically\n    \n    // realloc - resize\n    int new_size = n * 2;\n    int* expanded = (int*)realloc(arr, new_size * sizeof(int));\n    if (expanded != NULL) {\n        arr = expanded;\n        for (int i = n; i < new_size; i++) {\n            arr[i] = i * 10;\n        }\n        \n        printf(\"Expanded array: \");\n        for (int i = 0; i < new_size; i++) {\n            printf(\"%d \", arr[i]);\n        }\n        printf(\"\\n\");\n    }\n    \n    // Free memory\n    free(arr);\n    free(arr2);\n    \n    // 2D dynamic array\n    int rows = 3, cols = 4;\n    int** matrix = (int**)malloc(rows * sizeof(int*));\n    for (int i = 0; i < rows; i++) {\n        matrix[i] = (int*)malloc(cols * sizeof(int));\n    }\n    \n    // Use matrix...\n    \n    // Free 2D array\n    for (int i = 0; i < rows; i++) {\n        free(matrix[i]);\n    }\n    free(matrix);\n    \n    return 0;\n}", output: "Dynamic memory allocation with malloc, calloc, realloc, free." },
      ], notes: ["malloc raw memory allocate karta hai", "calloc zero-initialized memory deta hai", "free() memory release karta hai", "Always NULL check after allocation"],
      commonMistakes: [{ mistake: "free() bhoolna (memory leak)", correction: "Har malloc/calloc/realloc ke baad free() karein" }],
      interviewQuestions: ["malloc aur calloc mein kya antar hai?", "Memory leak kya hai?", "Dangling pointer kya hai?"],
      practiceExercises: [{ question: "Dynamic array implement karein jisme user input ke hisaab se array resize ho.", difficulty: "hard" }],
      quiz: { id: "quiz-c-dynamic", title: "Dynamic Memory Quiz", questions: [
        { id: "q1", question: "Memory free karne ke liye kaunsa function use hota hai?", options: ["free()", "delete()", "dealloc()", "release()"], correctAnswer: 0, explanation: "free() memory release karta hai." },
      ]}, }, duration: "20 min" },
      { id: "c-project", title: "Project - Library Management", description: "C mein library management project", order: 10, content: { introduction: "Ek console-based library management system banayenge jisme C ke sabhi concepts ka use hoga - structures, functions, file handling, dynamic memory, pointers.", sections: [
        { title: "Project Overview", explanation: "Library management system with book add, display, search, delete, issue, return. File I/O for persistent storage. Dynamic memory for flexibility. Structures for data organization.", example: "Features: Add Book, Display All, Search, Delete, Issue Book, Return Book" },
        { title: "Implementation", explanation: "Struct Book with id, title, author, year, isIssued. Array of structures (dynamic). File handling for save/load. Functions for each operation. Menu-driven interface.", example: "Menu: 1. Add 2. Display 3. Search 4. Delete 5. Issue 6. Return 7. Exit" },
      ], codeExamples: [
        { title: "Library Management", language: "c", code: "#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\n#define MAX_BOOKS 100\n#define FILENAME \"library.dat\"\n\ntypedef struct {\n    int id;\n    char title[100];\n    char author[100];\n    int year;\n    int isIssued;  // 0 = available, 1 = issued\n} Book;\n\n// Global array\nBook* library = NULL;\nint bookCount = 0;\n\n// Function declarations\nvoid loadFromFile();\nvoid saveToFile();\nvoid addBook();\nvoid displayBooks();\nvoid searchBook();\nvoid deleteBook();\nvoid issueBook();\nvoid returnBook();\n\nint main() {\n    loadFromFile();\n    \n    int choice;\n    while (1) {\n        printf(\"\\n===== LIBRARY MANAGEMENT SYSTEM =====\\n\");\n        printf(\"1. Book Add Karein\\n\");\n        printf(\"2. Books Display Karein\\n\");\n        printf(\"3. Book Search Karein\\n\");\n        printf(\"4. Book Delete Karein\\n\");\n        printf(\"5. Book Issue Karein\\n\");\n        printf(\"6. Book Return Karein\\n\");\n        printf(\"7. Exit\\n\");\n        printf(\"Choice: \");\n        scanf(\"%d\", &choice);\n        \n        switch (choice) {\n            case 1: addBook(); break;\n            case 2: displayBooks(); break;\n            case 3: searchBook(); break;\n            case 4: deleteBook(); break;\n            case 5: issueBook(); break;\n            case 6: returnBook(); break;\n            case 7: \n                saveToFile();\n                free(library);\n                printf(\"Goodbye!\\n\");\n                return 0;\n            default: printf(\"Invalid choice!\\n\");\n        }\n    }\n}\n\nvoid addBook() {\n    library = realloc(library, (bookCount + 1) * sizeof(Book));\n    if (library == NULL) { printf(\"Memory error!\\n\"); return; }\n    \n    Book* b = &library[bookCount];\n    b->id = bookCount + 1;\n    \n    printf(\"Title: \");\n    getchar(); // consume newline\n    fgets(b->title, 100, stdin);\n    b->title[strcspn(b->title, \"\\n\")] = 0;\n    \n    printf(\"Author: \");\n    fgets(b->author, 100, stdin);\n    b->author[strcspn(b->author, \"\\n\")] = 0;\n    \n    printf(\"Year: \");\n    scanf(\"%d\", &b->year);\n    b->isIssued = 0;\n    \n    bookCount++;\n    printf(\"Book added! ID: %d\\n\", b->id);\n    saveToFile();\n}\n\nvoid displayBooks() {\n    if (bookCount == 0) {\n        printf(\"Library mein koi book nahi hai.\\n\");\n        return;\n    }\n    \n    printf(\"\\n%-5s %-30s %-20s %-6s %-10s\\n\", \"ID\", \"Title\", \"Author\", \"Year\", \"Status\");\n    printf(\"%-5s %-30s %-20s %-6s %-10s\\n\", \"----\", \"----\", \"------\", \"----\", \"------\");\n    \n    for (int i = 0; i < bookCount; i++) {\n        Book* b = &library[i];\n        printf(\"%-5d %-30s %-20s %-6d %-10s\\n\", \n            b->id, b->title, b->author, b->year,\n            b->isIssued ? \"Issued\" : \"Available\");\n    }\n}\n\nvoid searchBook() {\n    char query[100];\n    printf(\"Search title/author: \");\n    getchar();\n    fgets(query, 100, stdin);\n    query[strcspn(query, \"\\n\")] = 0;\n    \n    int found = 0;\n    for (int i = 0; i < bookCount; i++) {\n        Book* b = &library[i];\n        if (strstr(b->title, query) || strstr(b->author, query)) {\n            printf(\"%d: %s by %s (%d) - %s\\n\", b->id, b->title, b->author, b->year,\n                b->isIssued ? \"Issued\" : \"Available\");\n            found = 1;\n        }\n    }\n    \n    if (!found) printf(\"Koi book nahi milli.\\n\");\n}\n\nvoid deleteBook() {\n    int id;\n    printf(\"Book ID delete karne ke liye: \");\n    scanf(\"%d\", &id);\n    \n    int found = 0;\n    for (int i = 0; i < bookCount; i++) {\n        if (library[i].id == id) {\n            for (int j = i; j < bookCount - 1; j++) {\n                library[j] = library[j + 1];\n            }\n            bookCount--;\n            library = realloc(library, bookCount * sizeof(Book));\n            printf(\"Book deleted!\\n\");\n            found = 1;\n            break;\n        }\n    }\n    \n    if (!found) printf(\"Book nahi milli.\\n\");\n    saveToFile();\n}\n\nvoid issueBook() {\n    int id;\n    printf(\"Book ID issue karne ke liye: \");\n    scanf(\"%d\", &id);\n    \n    for (int i = 0; i < bookCount; i++) {\n        if (library[i].id == id) {\n            if (library[i].isIssued) {\n                printf(\"Book already issued hai!\\n\");\n            } else {\n                library[i].isIssued = 1;\n                printf(\"Book issued!\\n\");\n                saveToFile();\n            }\n            return;\n        }\n    }\n    printf(\"Book nahi milli.\\n\");\n}\n\nvoid returnBook() {\n    int id;\n    printf(\"Book ID return karne ke liye: \");\n    scanf(\"%d\", &id);\n    \n    for (int i = 0; i < bookCount; i++) {\n        if (library[i].id == id) {\n            if (!library[i].isIssued) {\n                printf(\"Book already available hai!\\n\");\n            } else {\n                library[i].isIssued = 0;\n                printf(\"Book returned!\\n\");\n                saveToFile();\n            }\n            return;\n        }\n    }\n    printf(\"Book nahi milli.\\n\");\n}\n\nvoid loadFromFile() {\n    FILE* f = fopen(FILENAME, \"rb\");\n    if (f == NULL) return;\n    \n    fread(&bookCount, sizeof(int), 1, f);\n    library = malloc(bookCount * sizeof(Book));\n    if (library != NULL) {\n        fread(library, sizeof(Book), bookCount, f);\n    }\n    fclose(f);\n}\n\nvoid saveToFile() {\n    FILE* f = fopen(FILENAME, \"wb\");\n    if (f == NULL) return;\n    \n    fwrite(&bookCount, sizeof(int), 1, f);\n    fwrite(library, sizeof(Book), bookCount, f);\n    fclose(f);\n}", output: "Complete library management system in C." },
      ], notes: ["Structures for data organization", "Dynamic memory for flexible storage", "File I/O for persistence", "Menu-driven user interface"],
      commonMistakes: [{ mistake: "File operations ke baad file close na karna", correction: "Always fclose() after file operations" }],
      interviewQuestions: ["Project ka architecture kya hai?", "Dynamic memory kyun use ki?", "File handling kaise implement ki?"],
      practiceExercises: [{ question: "Library system mein late fee calculation feature add karein.", difficulty: "hard" }],
      quiz: { id: "quiz-c-project", title: "C Project Quiz", questions: [
        { id: "q1", question: "Dynamic array resize ke liye kaunsa function use hota hai?", options: ["malloc", "calloc", "realloc", "free"], correctAnswer: 2, explanation: "realloc dynamic array resize karta hai." },
      ]}, }, duration: "30 min" },
    ],
  }],
}
